<template><div><h1 id="acorn" tabindex="-1"><a class="header-anchor" href="#acorn" aria-hidden="true">#</a> Acorn</h1>
<p>A tiny, fast JavaScript parser written in JavaScript.</p>
<h2 id="community" tabindex="-1"><a class="header-anchor" href="#community" aria-hidden="true">#</a> Community</h2>
<p>Acorn is open source software released under an
<a href="https://github.com/acornjs/acorn/blob/master/acorn/LICENSE" target="_blank" rel="noopener noreferrer">MIT license<ExternalLinkIcon/></a>.</p>
<p>You are welcome to
<a href="https://github.com/acornjs/acorn/issues" target="_blank" rel="noopener noreferrer">report bugs<ExternalLinkIcon/></a> or create pull
requests on <a href="https://github.com/acornjs/acorn" target="_blank" rel="noopener noreferrer">github<ExternalLinkIcon/></a>. For questions
and discussion, please use the
<a href="https://discuss.ternjs.net" target="_blank" rel="noopener noreferrer">Tern discussion forum<ExternalLinkIcon/></a>.</p>
<h2 id="installation" tabindex="-1"><a class="header-anchor" href="#installation" aria-hidden="true">#</a> Installation</h2>
<p>The easiest way to install acorn is from <a href="https://www.npmjs.com/" target="_blank" rel="noopener noreferrer"><code v-pre>npm</code><ExternalLinkIcon/></a>:</p>
<div class="language-bash ext-sh line-numbers-mode"><pre v-pre class="language-bash"><code><span class="token function">npm</span> <span class="token function">install</span> acorn
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Alternately, you can download the source and build acorn yourself:</p>
<div class="language-bash ext-sh line-numbers-mode"><pre v-pre class="language-bash"><code><span class="token function">git</span> clone https://github.com/acornjs/acorn.git
<span class="token builtin class-name">cd</span> acorn
<span class="token function">npm</span> <span class="token function">install</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="interface" tabindex="-1"><a class="header-anchor" href="#interface" aria-hidden="true">#</a> Interface</h2>
<p><strong>parse</strong><code v-pre>(input, options)</code> is the main interface to the library. The
<code v-pre>input</code> parameter is a string, <code v-pre>options</code> must be an object setting
some of the options listed below. The return value will be an abstract
syntax tree object as specified by the <a href="https://github.com/estree/estree" target="_blank" rel="noopener noreferrer">ESTree
spec<ExternalLinkIcon/></a>.</p>
<div class="language-javascript ext-js line-numbers-mode"><pre v-pre class="language-javascript"><code><span class="token keyword">let</span> acorn <span class="token operator">=</span> <span class="token function">require</span><span class="token punctuation">(</span><span class="token string">"acorn"</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span>acorn<span class="token punctuation">.</span><span class="token function">parse</span><span class="token punctuation">(</span><span class="token string">"1 + 1"</span><span class="token punctuation">,</span> <span class="token punctuation">{</span><span class="token literal-property property">ecmaVersion</span><span class="token operator">:</span> <span class="token number">2020</span><span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>When encountering a syntax error, the parser will raise a
<code v-pre>SyntaxError</code> object with a meaningful message. The error object will
have a <code v-pre>pos</code> property that indicates the string offset at which the
error occurred, and a <code v-pre>loc</code> object that contains a <code v-pre>{line, column}</code>
object referring to that same position.</p>
<p>Options are provided by in a second argument, which should be an
object containing any of these fields (only <code v-pre>ecmaVersion</code> is
required):</p>
<ul>
<li>
<p><strong>ecmaVersion</strong>: Indicates the ECMAScript version to parse. Must be
either 3, 5, 6 (or 2015), 7 (2016), 8 (2017), 9 (2018), 10 (2019),
11 (2020), 12 (2021), 13 (2022, partial support)
or <code v-pre>&quot;latest&quot;</code> (the latest the library supports). This influences
support for strict mode, the set of reserved words, and support
for new syntax features.</p>
<p><strong>NOTE</strong>: Only 'stage 4' (finalized) ECMAScript features are being
implemented by Acorn. Other proposed new features must be
implemented through plugins.</p>
</li>
<li>
<p><strong>sourceType</strong>: Indicate the mode the code should be parsed in. Can be
either <code v-pre>&quot;script&quot;</code> or <code v-pre>&quot;module&quot;</code>. This influences global strict mode
and parsing of <code v-pre>import</code> and <code v-pre>export</code> declarations.</p>
<p><strong>NOTE</strong>: If set to <code v-pre>&quot;module&quot;</code>, then static <code v-pre>import</code> / <code v-pre>export</code> syntax
will be valid, even if <code v-pre>ecmaVersion</code> is less than 6.</p>
</li>
<li>
<p><strong>onInsertedSemicolon</strong>: If given a callback, that callback will be
called whenever a missing semicolon is inserted by the parser. The
callback will be given the character offset of the point where the
semicolon is inserted as argument, and if <code v-pre>locations</code> is on, also a
<code v-pre>{line, column}</code> object representing this position.</p>
</li>
<li>
<p><strong>onTrailingComma</strong>: Like <code v-pre>onInsertedSemicolon</code>, but for trailing
commas.</p>
</li>
<li>
<p><strong>allowReserved</strong>: If <code v-pre>false</code>, using a reserved word will generate
an error. Defaults to <code v-pre>true</code> for <code v-pre>ecmaVersion</code> 3, <code v-pre>false</code> for higher
versions. When given the value <code v-pre>&quot;never&quot;</code>, reserved words and
keywords can also not be used as property names (as in Internet
Explorer's old parser).</p>
</li>
<li>
<p><strong>allowReturnOutsideFunction</strong>: By default, a return statement at
the top level raises an error. Set this to <code v-pre>true</code> to accept such
code.</p>
</li>
<li>
<p><strong>allowImportExportEverywhere</strong>: By default, <code v-pre>import</code> and <code v-pre>export</code>
declarations can only appear at a program's top level. Setting this
option to <code v-pre>true</code> allows them anywhere where a statement is allowed,
and also allows <code v-pre>import.meta</code> expressions to appear in scripts
(when <code v-pre>sourceType</code> is not <code v-pre>&quot;module&quot;</code>).</p>
</li>
<li>
<p><strong>allowAwaitOutsideFunction</strong>: If <code v-pre>false</code>, <code v-pre>await</code> expressions can
only appear inside <code v-pre>async</code> functions. Defaults to <code v-pre>true</code> for
<code v-pre>ecmaVersion</code> 2022 and later, <code v-pre>false</code> for lower versions. Setting this option to
<code v-pre>true</code> allows to have top-level <code v-pre>await</code> expressions. They are
still not allowed in non-<code v-pre>async</code> functions, though.</p>
</li>
<li>
<p><strong>allowSuperOutsideMethod</strong>: By default, <code v-pre>super</code> outside a method
raises an error. Set this to <code v-pre>true</code> to accept such code.</p>
</li>
<li>
<p><strong>allowHashBang</strong>: When this is enabled (off by default), if the
code starts with the characters <code v-pre>#!</code> (as in a shellscript), the
first line will be treated as a comment.</p>
</li>
<li>
<p><strong>locations</strong>: When <code v-pre>true</code>, each node has a <code v-pre>loc</code> object attached
with <code v-pre>start</code> and <code v-pre>end</code> subobjects, each of which contains the
one-based line and zero-based column numbers in <code v-pre>{line, column}</code>
form. Default is <code v-pre>false</code>.</p>
</li>
<li>
<p><strong>onToken</strong>: If a function is passed for this option, each found
token will be passed in same format as tokens returned from
<code v-pre>tokenizer().getToken()</code>.</p>
<p>If array is passed, each found token is pushed to it.</p>
<p>Note that you are not allowed to call the parser from the
callback—that will corrupt its internal state.</p>
</li>
<li>
<p><strong>onComment</strong>: If a function is passed for this option, whenever a
comment is encountered the function will be called with the
following parameters:</p>
<ul>
<li><code v-pre>block</code>: <code v-pre>true</code> if the comment is a block comment, false if it
is a line comment.</li>
<li><code v-pre>text</code>: The content of the comment.</li>
<li><code v-pre>start</code>: Character offset of the start of the comment.</li>
<li><code v-pre>end</code>: Character offset of the end of the comment.</li>
</ul>
<p>When the <code v-pre>locations</code> options is on, the <code v-pre>{line, column}</code> locations
of the comment’s start and end are passed as two additional
parameters.</p>
<p>If array is passed for this option, each found comment is pushed
to it as object in Esprima format:</p>
<div class="language-javascript ext-js line-numbers-mode"><pre v-pre class="language-javascript"><code><span class="token punctuation">{</span>
  <span class="token string-property property">"type"</span><span class="token operator">:</span> <span class="token string">"Line"</span> <span class="token operator">|</span> <span class="token string">"Block"</span><span class="token punctuation">,</span>
  <span class="token string-property property">"value"</span><span class="token operator">:</span> <span class="token string">"comment text"</span><span class="token punctuation">,</span>
  <span class="token string-property property">"start"</span><span class="token operator">:</span> Number<span class="token punctuation">,</span>
  <span class="token string-property property">"end"</span><span class="token operator">:</span> Number<span class="token punctuation">,</span>
  <span class="token comment">// If `locations` option is on:</span>
  <span class="token string-property property">"loc"</span><span class="token operator">:</span> <span class="token punctuation">{</span>
    <span class="token string-property property">"start"</span><span class="token operator">:</span> <span class="token punctuation">{</span><span class="token literal-property property">line</span><span class="token operator">:</span> Number<span class="token punctuation">,</span> <span class="token literal-property property">column</span><span class="token operator">:</span> Number<span class="token punctuation">}</span>
    <span class="token string-property property">"end"</span><span class="token operator">:</span> <span class="token punctuation">{</span><span class="token literal-property property">line</span><span class="token operator">:</span> Number<span class="token punctuation">,</span> <span class="token literal-property property">column</span><span class="token operator">:</span> Number<span class="token punctuation">}</span>
  <span class="token punctuation">}</span><span class="token punctuation">,</span>
  <span class="token comment">// If `ranges` option is on:</span>
  <span class="token string-property property">"range"</span><span class="token operator">:</span> <span class="token punctuation">[</span>Number<span class="token punctuation">,</span> Number<span class="token punctuation">]</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Note that you are not allowed to call the parser from the
callback—that will corrupt its internal state.</p>
</li>
<li>
<p><strong>ranges</strong>: Nodes have their start and end characters offsets
recorded in <code v-pre>start</code> and <code v-pre>end</code> properties (directly on the node,
rather than the <code v-pre>loc</code> object, which holds line/column data. To also
add a
<a href="https://bugzilla.mozilla.org/show_bug.cgi?id=745678" target="_blank" rel="noopener noreferrer">semi-standardized<ExternalLinkIcon/></a>
<code v-pre>range</code> property holding a <code v-pre>[start, end]</code> array with the same
numbers, set the <code v-pre>ranges</code> option to <code v-pre>true</code>.</p>
</li>
<li>
<p><strong>program</strong>: It is possible to parse multiple files into a single
AST by passing the tree produced by parsing the first file as the
<code v-pre>program</code> option in subsequent parses. This will add the toplevel
forms of the parsed file to the &quot;Program&quot; (top) node of an existing
parse tree.</p>
</li>
<li>
<p><strong>sourceFile</strong>: When the <code v-pre>locations</code> option is <code v-pre>true</code>, you can pass
this option to add a <code v-pre>source</code> attribute in every node’s <code v-pre>loc</code>
object. Note that the contents of this option are not examined or
processed in any way; you are free to use whatever format you
choose.</p>
</li>
<li>
<p><strong>directSourceFile</strong>: Like <code v-pre>sourceFile</code>, but a <code v-pre>sourceFile</code> property
will be added (regardless of the <code v-pre>location</code> option) directly to the
nodes, rather than the <code v-pre>loc</code> object.</p>
</li>
<li>
<p><strong>preserveParens</strong>: If this option is <code v-pre>true</code>, parenthesized expressions
are represented by (non-standard) <code v-pre>ParenthesizedExpression</code> nodes
that have a single <code v-pre>expression</code> property containing the expression
inside parentheses.</p>
</li>
</ul>
<p><strong>parseExpressionAt</strong><code v-pre>(input, offset, options)</code> will parse a single
expression in a string, and return its AST. It will not complain if
there is more of the string left after the expression.</p>
<p><strong>tokenizer</strong><code v-pre>(input, options)</code> returns an object with a <code v-pre>getToken</code>
method that can be called repeatedly to get the next token, a <code v-pre>{start, end, type, value}</code> object (with added <code v-pre>loc</code> property when the
<code v-pre>locations</code> option is enabled and <code v-pre>range</code> property when the <code v-pre>ranges</code>
option is enabled). When the token's type is <code v-pre>tokTypes.eof</code>, you
should stop calling the method, since it will keep returning that same
token forever.</p>
<p>In ES6 environment, returned result can be used as any other
protocol-compliant iterable:</p>
<div class="language-javascript ext-js line-numbers-mode"><pre v-pre class="language-javascript"><code><span class="token keyword">for</span> <span class="token punctuation">(</span><span class="token keyword">let</span> token <span class="token keyword">of</span> acorn<span class="token punctuation">.</span><span class="token function">tokenizer</span><span class="token punctuation">(</span>str<span class="token punctuation">)</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
  <span class="token comment">// iterate over the tokens</span>
<span class="token punctuation">}</span>

<span class="token comment">// transform code to array of tokens:</span>
<span class="token keyword">var</span> tokens <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token operator">...</span>acorn<span class="token punctuation">.</span><span class="token function">tokenizer</span><span class="token punctuation">(</span>str<span class="token punctuation">)</span><span class="token punctuation">]</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><strong>tokTypes</strong> holds an object mapping names to the token type objects
that end up in the <code v-pre>type</code> properties of tokens.</p>
<p><strong>getLineInfo</strong><code v-pre>(input, offset)</code> can be used to get a <code v-pre>{line, column}</code> object for a given program string and offset.</p>
<h3 id="the-parser-class" tabindex="-1"><a class="header-anchor" href="#the-parser-class" aria-hidden="true">#</a> The <code v-pre>Parser</code> class</h3>
<p>Instances of the <strong><code v-pre>Parser</code></strong> class contain all the state and logic
that drives a parse. It has static methods <code v-pre>parse</code>,
<code v-pre>parseExpressionAt</code>, and <code v-pre>tokenizer</code> that match the top-level
functions by the same name.</p>
<p>When extending the parser with plugins, you need to call these methods
on the extended version of the class. To extend a parser with plugins,
you can use its static <code v-pre>extend</code> method.</p>
<div class="language-javascript ext-js line-numbers-mode"><pre v-pre class="language-javascript"><code><span class="token keyword">var</span> acorn <span class="token operator">=</span> <span class="token function">require</span><span class="token punctuation">(</span><span class="token string">"acorn"</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token keyword">var</span> jsx <span class="token operator">=</span> <span class="token function">require</span><span class="token punctuation">(</span><span class="token string">"acorn-jsx"</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token keyword">var</span> JSXParser <span class="token operator">=</span> acorn<span class="token punctuation">.</span>Parser<span class="token punctuation">.</span><span class="token function">extend</span><span class="token punctuation">(</span><span class="token function">jsx</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
JSXParser<span class="token punctuation">.</span><span class="token function">parse</span><span class="token punctuation">(</span><span class="token string">"foo(&lt;bar/>)"</span><span class="token punctuation">,</span> <span class="token punctuation">{</span><span class="token literal-property property">ecmaVersion</span><span class="token operator">:</span> <span class="token number">2020</span><span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The <code v-pre>extend</code> method takes any number of plugin values, and returns a
new <code v-pre>Parser</code> class that includes the extra parser logic provided by
the plugins.</p>
<h2 id="command-line-interface" tabindex="-1"><a class="header-anchor" href="#command-line-interface" aria-hidden="true">#</a> Command line interface</h2>
<p>The <code v-pre>bin/acorn</code> utility can be used to parse a file from the command
line. It accepts as arguments its input file and the following
options:</p>
<ul>
<li>
<p><code v-pre>--ecma3|--ecma5|--ecma6|--ecma7|--ecma8|--ecma9|--ecma10</code>: Sets the ECMAScript version
to parse. Default is version 9.</p>
</li>
<li>
<p><code v-pre>--module</code>: Sets the parsing mode to <code v-pre>&quot;module&quot;</code>. Is set to <code v-pre>&quot;script&quot;</code> otherwise.</p>
</li>
<li>
<p><code v-pre>--locations</code>: Attaches a &quot;loc&quot; object to each node with &quot;start&quot; and
&quot;end&quot; subobjects, each of which contains the one-based line and
zero-based column numbers in <code v-pre>{line, column}</code> form.</p>
</li>
<li>
<p><code v-pre>--allow-hash-bang</code>: If the code starts with the characters #! (as
in a shellscript), the first line will be treated as a comment.</p>
</li>
<li>
<p><code v-pre>--allow-await-outside-function</code>: Allows top-level <code v-pre>await</code> expressions.
See the <code v-pre>allowAwaitOutsideFunction</code> option for more information.</p>
</li>
<li>
<p><code v-pre>--compact</code>: No whitespace is used in the AST output.</p>
</li>
<li>
<p><code v-pre>--silent</code>: Do not output the AST, just return the exit status.</p>
</li>
<li>
<p><code v-pre>--help</code>: Print the usage information and quit.</p>
</li>
</ul>
<p>The utility spits out the syntax tree as JSON data.</p>
<h2 id="existing-plugins" tabindex="-1"><a class="header-anchor" href="#existing-plugins" aria-hidden="true">#</a> Existing plugins</h2>
<ul>
<li><a href="https://github.com/RReverser/acorn-jsx" target="_blank" rel="noopener noreferrer"><code v-pre>acorn-jsx</code><ExternalLinkIcon/></a>: Parse <a href="https://github.com/facebook/jsx" target="_blank" rel="noopener noreferrer">Facebook JSX syntax extensions<ExternalLinkIcon/></a></li>
</ul>
</div></template>
