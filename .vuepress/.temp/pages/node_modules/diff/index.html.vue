<template><div><h1 id="jsdiff" tabindex="-1"><a class="header-anchor" href="#jsdiff" aria-hidden="true">#</a> jsdiff</h1>
<p><a href="http://travis-ci.org/kpdecker/jsdiff" target="_blank" rel="noopener noreferrer"><img src="https://secure.travis-ci.org/kpdecker/jsdiff.svg" alt="Build Status"><ExternalLinkIcon/></a>
<a href="https://saucelabs.com/u/jsdiff" target="_blank" rel="noopener noreferrer"><img src="https://saucelabs.com/buildstatus/jsdiff" alt="Sauce Test Status"><ExternalLinkIcon/></a></p>
<p>A javascript text differencing implementation.</p>
<p>Based on the algorithm proposed in
<a href="http://citeseerx.ist.psu.edu/viewdoc/summary?doi=10.1.1.4.6927" target="_blank" rel="noopener noreferrer">&quot;An O(ND) Difference Algorithm and its Variations&quot; (Myers, 1986)<ExternalLinkIcon/></a>.</p>
<h2 id="installation" tabindex="-1"><a class="header-anchor" href="#installation" aria-hidden="true">#</a> Installation</h2>
<div class="language-bash ext-sh line-numbers-mode"><pre v-pre class="language-bash"><code><span class="token function">npm</span> <span class="token function">install</span> <span class="token function">diff</span> --save
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><h2 id="api" tabindex="-1"><a class="header-anchor" href="#api" aria-hidden="true">#</a> API</h2>
<ul>
<li>
<p><code v-pre>JsDiff.diffChars(oldStr, newStr[, options])</code> - diffs two blocks of text, comparing character by character.</p>
<p>Returns a list of change objects (See below).</p>
<p>Options</p>
<ul>
<li><code v-pre>ignoreCase</code>: <code v-pre>true</code> to ignore casing difference. Defaults to <code v-pre>false</code>.</li>
</ul>
</li>
<li>
<p><code v-pre>JsDiff.diffWords(oldStr, newStr[, options])</code> - diffs two blocks of text, comparing word by word, ignoring whitespace.</p>
<p>Returns a list of change objects (See below).</p>
<p>Options</p>
<ul>
<li><code v-pre>ignoreCase</code>: Same as in <code v-pre>diffChars</code>.</li>
</ul>
</li>
<li>
<p><code v-pre>JsDiff.diffWordsWithSpace(oldStr, newStr[, options])</code> - diffs two blocks of text, comparing word by word, treating whitespace as significant.</p>
<p>Returns a list of change objects (See below).</p>
</li>
<li>
<p><code v-pre>JsDiff.diffLines(oldStr, newStr[, options])</code> - diffs two blocks of text, comparing line by line.</p>
<p>Options</p>
<ul>
<li><code v-pre>ignoreWhitespace</code>: <code v-pre>true</code> to ignore leading and trailing whitespace. This is the same as <code v-pre>diffTrimmedLines</code></li>
<li><code v-pre>newlineIsToken</code>: <code v-pre>true</code> to treat newline characters as separate tokens.  This allows for changes to the newline structure to occur independently of the line content and to be treated as such. In general this is the more human friendly form of <code v-pre>diffLines</code> and <code v-pre>diffLines</code> is better suited for patches and other computer friendly output.</li>
</ul>
<p>Returns a list of change objects (See below).</p>
</li>
<li>
<p><code v-pre>JsDiff.diffTrimmedLines(oldStr, newStr[, options])</code> - diffs two blocks of text, comparing line by line, ignoring leading and trailing whitespace.</p>
<p>Returns a list of change objects (See below).</p>
</li>
<li>
<p><code v-pre>JsDiff.diffSentences(oldStr, newStr[, options])</code> - diffs two blocks of text, comparing sentence by sentence.</p>
<p>Returns a list of change objects (See below).</p>
</li>
<li>
<p><code v-pre>JsDiff.diffCss(oldStr, newStr[, options])</code> - diffs two blocks of text, comparing CSS tokens.</p>
<p>Returns a list of change objects (See below).</p>
</li>
<li>
<p><code v-pre>JsDiff.diffJson(oldObj, newObj[, options])</code> - diffs two JSON objects, comparing the fields defined on each. The order of fields, etc does not matter in this comparison.</p>
<p>Returns a list of change objects (See below).</p>
</li>
<li>
<p><code v-pre>JsDiff.diffArrays(oldArr, newArr[, options])</code> - diffs two arrays, comparing each item for strict equality (===).</p>
<p>Options</p>
<ul>
<li><code v-pre>comparator</code>: <code v-pre>function(left, right)</code> for custom equality checks</li>
</ul>
<p>Returns a list of change objects (See below).</p>
</li>
<li>
<p><code v-pre>JsDiff.createTwoFilesPatch(oldFileName, newFileName, oldStr, newStr, oldHeader, newHeader)</code> - creates a unified diff patch.</p>
<p>Parameters:</p>
<ul>
<li><code v-pre>oldFileName</code> : String to be output in the filename section of the patch for the removals</li>
<li><code v-pre>newFileName</code> : String to be output in the filename section of the patch for the additions</li>
<li><code v-pre>oldStr</code> : Original string value</li>
<li><code v-pre>newStr</code> : New string value</li>
<li><code v-pre>oldHeader</code> : Additional information to include in the old file header</li>
<li><code v-pre>newHeader</code> : Additional information to include in the new file header</li>
<li><code v-pre>options</code> : An object with options. Currently, only <code v-pre>context</code> is supported and describes how many lines of context should be included.</li>
</ul>
</li>
<li>
<p><code v-pre>JsDiff.createPatch(fileName, oldStr, newStr, oldHeader, newHeader)</code> - creates a unified diff patch.</p>
<p>Just like JsDiff.createTwoFilesPatch, but with oldFileName being equal to newFileName.</p>
</li>
<li>
<p><code v-pre>JsDiff.structuredPatch(oldFileName, newFileName, oldStr, newStr, oldHeader, newHeader, options)</code> - returns an object with an array of hunk objects.</p>
<p>This method is similar to createTwoFilesPatch, but returns a data structure
suitable for further processing. Parameters are the same as createTwoFilesPatch. The data structure returned may look like this:</p>
<div class="language-javascript ext-js line-numbers-mode"><pre v-pre class="language-javascript"><code><span class="token punctuation">{</span>
  <span class="token literal-property property">oldFileName</span><span class="token operator">:</span> <span class="token string">'oldfile'</span><span class="token punctuation">,</span> <span class="token literal-property property">newFileName</span><span class="token operator">:</span> <span class="token string">'newfile'</span><span class="token punctuation">,</span>
  <span class="token literal-property property">oldHeader</span><span class="token operator">:</span> <span class="token string">'header1'</span><span class="token punctuation">,</span> <span class="token literal-property property">newHeader</span><span class="token operator">:</span> <span class="token string">'header2'</span><span class="token punctuation">,</span>
  <span class="token literal-property property">hunks</span><span class="token operator">:</span> <span class="token punctuation">[</span><span class="token punctuation">{</span>
    <span class="token literal-property property">oldStart</span><span class="token operator">:</span> <span class="token number">1</span><span class="token punctuation">,</span> <span class="token literal-property property">oldLines</span><span class="token operator">:</span> <span class="token number">3</span><span class="token punctuation">,</span> <span class="token literal-property property">newStart</span><span class="token operator">:</span> <span class="token number">1</span><span class="token punctuation">,</span> <span class="token literal-property property">newLines</span><span class="token operator">:</span> <span class="token number">3</span><span class="token punctuation">,</span>
    <span class="token literal-property property">lines</span><span class="token operator">:</span> <span class="token punctuation">[</span><span class="token string">' line2'</span><span class="token punctuation">,</span> <span class="token string">' line3'</span><span class="token punctuation">,</span> <span class="token string">'-line4'</span><span class="token punctuation">,</span> <span class="token string">'+line5'</span><span class="token punctuation">,</span> <span class="token string">'\\ No newline at end of file'</span><span class="token punctuation">]</span><span class="token punctuation">,</span>
  <span class="token punctuation">}</span><span class="token punctuation">]</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></li>
<li>
<p><code v-pre>JsDiff.applyPatch(source, patch[, options])</code> - applies a unified diff patch.</p>
<p>Return a string containing new version of provided data. <code v-pre>patch</code> may be a string diff or the output from the <code v-pre>parsePatch</code> or <code v-pre>structuredPatch</code> methods.</p>
<p>The optional <code v-pre>options</code> object may have the following keys:</p>
<ul>
<li><code v-pre>fuzzFactor</code>: Number of lines that are allowed to differ before rejecting a patch. Defaults to 0.</li>
<li><code v-pre>compareLine(lineNumber, line, operation, patchContent)</code>: Callback used to compare to given lines to determine if they should be considered equal when patching. Defaults to strict equality but may be overridden to provide fuzzier comparison. Should return false if the lines should be rejected.</li>
</ul>
</li>
<li>
<p><code v-pre>JsDiff.applyPatches(patch, options)</code> - applies one or more patches.</p>
<p>This method will iterate over the contents of the patch and apply to data provided through callbacks. The general flow for each patch index is:</p>
<ul>
<li><code v-pre>options.loadFile(index, callback)</code> is called. The caller should then load the contents of the file and then pass that to the <code v-pre>callback(err, data)</code> callback. Passing an <code v-pre>err</code> will terminate further patch execution.</li>
<li><code v-pre>options.patched(index, content, callback)</code> is called once the patch has been applied. <code v-pre>content</code> will be the return value from <code v-pre>applyPatch</code>. When it's ready, the caller should call <code v-pre>callback(err)</code> callback. Passing an <code v-pre>err</code> will terminate further patch execution.</li>
</ul>
<p>Once all patches have been applied or an error occurs, the <code v-pre>options.complete(err)</code> callback is made.</p>
</li>
<li>
<p><code v-pre>JsDiff.parsePatch(diffStr)</code> - Parses a patch into structured data</p>
<p>Return a JSON object representation of the a patch, suitable for use with the <code v-pre>applyPatch</code> method. This parses to the same structure returned by <code v-pre>JsDiff.structuredPatch</code>.</p>
</li>
<li>
<p><code v-pre>convertChangesToXML(changes)</code> - converts a list of changes to a serialized XML format</p>
</li>
</ul>
<p>All methods above which accept the optional <code v-pre>callback</code> method will run in sync mode when that parameter is omitted and in async mode when supplied. This allows for larger diffs without blocking the event loop. This may be passed either directly as the final parameter or as the <code v-pre>callback</code> field in the <code v-pre>options</code> object.</p>
<h3 id="change-objects" tabindex="-1"><a class="header-anchor" href="#change-objects" aria-hidden="true">#</a> Change Objects</h3>
<p>Many of the methods above return change objects. These objects consist of the following fields:</p>
<ul>
<li><code v-pre>value</code>: Text content</li>
<li><code v-pre>added</code>: True if the value was inserted into the new string</li>
<li><code v-pre>removed</code>: True if the value was removed from the old string</li>
</ul>
<p>Note that some cases may omit a particular flag field. Comparison on the flag fields should always be done in a truthy or falsy manner.</p>
<h2 id="examples" tabindex="-1"><a class="header-anchor" href="#examples" aria-hidden="true">#</a> Examples</h2>
<p>Basic example in Node</p>
<div class="language-javascript ext-js line-numbers-mode"><pre v-pre class="language-javascript"><code><span class="token function">require</span><span class="token punctuation">(</span><span class="token string">'colors'</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token keyword">var</span> jsdiff <span class="token operator">=</span> <span class="token function">require</span><span class="token punctuation">(</span><span class="token string">'diff'</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

<span class="token keyword">var</span> one <span class="token operator">=</span> <span class="token string">'beep boop'</span><span class="token punctuation">;</span>
<span class="token keyword">var</span> other <span class="token operator">=</span> <span class="token string">'beep boob blah'</span><span class="token punctuation">;</span>

<span class="token keyword">var</span> diff <span class="token operator">=</span> jsdiff<span class="token punctuation">.</span><span class="token function">diffChars</span><span class="token punctuation">(</span>one<span class="token punctuation">,</span> other<span class="token punctuation">)</span><span class="token punctuation">;</span>

diff<span class="token punctuation">.</span><span class="token function">forEach</span><span class="token punctuation">(</span><span class="token keyword">function</span><span class="token punctuation">(</span><span class="token parameter">part</span><span class="token punctuation">)</span><span class="token punctuation">{</span>
  <span class="token comment">// green for additions, red for deletions</span>
  <span class="token comment">// grey for common parts</span>
  <span class="token keyword">var</span> color <span class="token operator">=</span> part<span class="token punctuation">.</span>added <span class="token operator">?</span> <span class="token string">'green'</span> <span class="token operator">:</span>
    part<span class="token punctuation">.</span>removed <span class="token operator">?</span> <span class="token string">'red'</span> <span class="token operator">:</span> <span class="token string">'grey'</span><span class="token punctuation">;</span>
  process<span class="token punctuation">.</span>stderr<span class="token punctuation">.</span><span class="token function">write</span><span class="token punctuation">(</span>part<span class="token punctuation">.</span>value<span class="token punctuation">[</span>color<span class="token punctuation">]</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Running the above program should yield</p>
<img src="images/node_example.png" alt="Node Example">
<p>Basic example in a web page</p>
<div class="language-html ext-html line-numbers-mode"><pre v-pre class="language-html"><code><span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>pre</span> <span class="token attr-name">id</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">"</span>display<span class="token punctuation">"</span></span><span class="token punctuation">></span></span><span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>pre</span><span class="token punctuation">></span></span>
<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>script</span> <span class="token attr-name">src</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">"</span>diff.js<span class="token punctuation">"</span></span><span class="token punctuation">></span></span><span class="token script"></span><span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>script</span><span class="token punctuation">></span></span>
<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>script</span><span class="token punctuation">></span></span><span class="token script"><span class="token language-javascript">
<span class="token keyword">var</span> one <span class="token operator">=</span> <span class="token string">'beep boop'</span><span class="token punctuation">,</span>
    other <span class="token operator">=</span> <span class="token string">'beep boob blah'</span><span class="token punctuation">,</span>
    color <span class="token operator">=</span> <span class="token string">''</span><span class="token punctuation">,</span>
    span <span class="token operator">=</span> <span class="token keyword">null</span><span class="token punctuation">;</span>

<span class="token keyword">var</span> diff <span class="token operator">=</span> JsDiff<span class="token punctuation">.</span><span class="token function">diffChars</span><span class="token punctuation">(</span>one<span class="token punctuation">,</span> other<span class="token punctuation">)</span><span class="token punctuation">,</span>
    display <span class="token operator">=</span> document<span class="token punctuation">.</span><span class="token function">getElementById</span><span class="token punctuation">(</span><span class="token string">'display'</span><span class="token punctuation">)</span><span class="token punctuation">,</span>
    fragment <span class="token operator">=</span> document<span class="token punctuation">.</span><span class="token function">createDocumentFragment</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

diff<span class="token punctuation">.</span><span class="token function">forEach</span><span class="token punctuation">(</span><span class="token keyword">function</span><span class="token punctuation">(</span><span class="token parameter">part</span><span class="token punctuation">)</span><span class="token punctuation">{</span>
  <span class="token comment">// green for additions, red for deletions</span>
  <span class="token comment">// grey for common parts</span>
  color <span class="token operator">=</span> part<span class="token punctuation">.</span>added <span class="token operator">?</span> <span class="token string">'green'</span> <span class="token operator">:</span>
    part<span class="token punctuation">.</span>removed <span class="token operator">?</span> <span class="token string">'red'</span> <span class="token operator">:</span> <span class="token string">'grey'</span><span class="token punctuation">;</span>
  span <span class="token operator">=</span> document<span class="token punctuation">.</span><span class="token function">createElement</span><span class="token punctuation">(</span><span class="token string">'span'</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
  span<span class="token punctuation">.</span>style<span class="token punctuation">.</span>color <span class="token operator">=</span> color<span class="token punctuation">;</span>
  span<span class="token punctuation">.</span><span class="token function">appendChild</span><span class="token punctuation">(</span>document
    <span class="token punctuation">.</span><span class="token function">createTextNode</span><span class="token punctuation">(</span>part<span class="token punctuation">.</span>value<span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
  fragment<span class="token punctuation">.</span><span class="token function">appendChild</span><span class="token punctuation">(</span>span<span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

display<span class="token punctuation">.</span><span class="token function">appendChild</span><span class="token punctuation">(</span>fragment<span class="token punctuation">)</span><span class="token punctuation">;</span>
</span></span><span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>script</span><span class="token punctuation">></span></span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Open the above .html file in a browser and you should see</p>
<img src="images/web_example.png" alt="Node Example">
<p><strong><a href="http://kpdecker.github.com/jsdiff" target="_blank" rel="noopener noreferrer">Full online demo<ExternalLinkIcon/></a></strong></p>
<h2 id="compatibility" tabindex="-1"><a class="header-anchor" href="#compatibility" aria-hidden="true">#</a> Compatibility</h2>
<p><a href="https://saucelabs.com/u/jsdiff" target="_blank" rel="noopener noreferrer"><img src="https://saucelabs.com/browser-matrix/jsdiff.svg" alt="Sauce Test Status"><ExternalLinkIcon/></a></p>
<p>jsdiff supports all ES3 environments with some known issues on IE8 and below. Under these browsers some diff algorithms such as word diff and others may fail due to lack of support for capturing groups in the <code v-pre>split</code> operation.</p>
<h2 id="license" tabindex="-1"><a class="header-anchor" href="#license" aria-hidden="true">#</a> License</h2>
<p>See <a href="https://github.com/kpdecker/jsdiff/blob/master/LICENSE" target="_blank" rel="noopener noreferrer">LICENSE<ExternalLinkIcon/></a>.</p>
</div></template>
