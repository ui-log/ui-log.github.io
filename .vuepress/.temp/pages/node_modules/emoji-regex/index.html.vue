<template><div><h1 id="emoji-regex" tabindex="-1"><a class="header-anchor" href="#emoji-regex" aria-hidden="true">#</a> emoji-regex <a href="https://travis-ci.org/mathiasbynens/emoji-regex" target="_blank" rel="noopener noreferrer"><img src="https://travis-ci.org/mathiasbynens/emoji-regex.svg?branch=main" alt="Build status"><ExternalLinkIcon/></a></h1>
<p><u>emoji-regex</u> offers a regular expression to match all emoji symbols and sequences (including textual representations of emoji) as per the Unicode Standard.</p>
<p>This repository contains a script that generates this regular expression based on <a href="https://github.com/node-unicode/node-unicode-data" target="_blank" rel="noopener noreferrer">Unicode data<ExternalLinkIcon/></a>. Because of this, the regular expression can easily be updated whenever new emoji are added to the Unicode standard.</p>
<h2 id="installation" tabindex="-1"><a class="header-anchor" href="#installation" aria-hidden="true">#</a> Installation</h2>
<p>Via <a href="https://www.npmjs.com/" target="_blank" rel="noopener noreferrer">npm<ExternalLinkIcon/></a>:</p>
<div class="language-bash ext-sh line-numbers-mode"><pre v-pre class="language-bash"><code><span class="token function">npm</span> <span class="token function">install</span> emoji-regex
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>In <a href="https://nodejs.org/" target="_blank" rel="noopener noreferrer">Node.js<ExternalLinkIcon/></a>:</p>
<div class="language-javascript ext-js line-numbers-mode"><pre v-pre class="language-javascript"><code><span class="token keyword">const</span> emojiRegex <span class="token operator">=</span> <span class="token function">require</span><span class="token punctuation">(</span><span class="token string">'emoji-regex/RGI_Emoji.js'</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token comment">// Note: because the regular expression has the global flag set, this module</span>
<span class="token comment">// exports a function that returns the regex rather than exporting the regular</span>
<span class="token comment">// expression itself, to make it impossible to (accidentally) mutate the</span>
<span class="token comment">// original regular expression.</span>

<span class="token keyword">const</span> text <span class="token operator">=</span> <span class="token template-string"><span class="token template-punctuation string">`</span><span class="token string">
\u{231A}: ⌚ default emoji presentation character (Emoji_Presentation)
\u{2194}\u{FE0F}: ↔️ default text presentation character rendered as emoji
\u{1F469}: 👩 emoji modifier base (Emoji_Modifier_Base)
\u{1F469}\u{1F3FF}: 👩🏿 emoji modifier base followed by a modifier
</span><span class="token template-punctuation string">`</span></span><span class="token punctuation">;</span>

<span class="token keyword">const</span> regex <span class="token operator">=</span> <span class="token function">emojiRegex</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token keyword">let</span> match<span class="token punctuation">;</span>
<span class="token keyword">while</span> <span class="token punctuation">(</span>match <span class="token operator">=</span> regex<span class="token punctuation">.</span><span class="token function">exec</span><span class="token punctuation">(</span>text<span class="token punctuation">)</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
  <span class="token keyword">const</span> emoji <span class="token operator">=</span> match<span class="token punctuation">[</span><span class="token number">0</span><span class="token punctuation">]</span><span class="token punctuation">;</span>
  console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span><span class="token template-string"><span class="token template-punctuation string">`</span><span class="token string">Matched sequence </span><span class="token interpolation"><span class="token interpolation-punctuation punctuation">${</span> emoji <span class="token interpolation-punctuation punctuation">}</span></span><span class="token string"> — code points: </span><span class="token interpolation"><span class="token interpolation-punctuation punctuation">${</span> <span class="token punctuation">[</span><span class="token operator">...</span>emoji<span class="token punctuation">]</span><span class="token punctuation">.</span>length <span class="token interpolation-punctuation punctuation">}</span></span><span class="token template-punctuation string">`</span></span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Console output:</p>
<div class="language-text ext-text line-numbers-mode"><pre v-pre class="language-text"><code>Matched sequence ⌚ — code points: 1
Matched sequence ⌚ — code points: 1
Matched sequence ↔️ — code points: 2
Matched sequence ↔️ — code points: 2
Matched sequence 👩 — code points: 1
Matched sequence 👩 — code points: 1
Matched sequence 👩🏿 — code points: 2
Matched sequence 👩🏿 — code points: 2
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="regular-expression-flavors" tabindex="-1"><a class="header-anchor" href="#regular-expression-flavors" aria-hidden="true">#</a> Regular expression flavors</h2>
<p>The package comes with three distinct regular expressions:</p>
<div class="language-javascript ext-js line-numbers-mode"><pre v-pre class="language-javascript"><code><span class="token comment">// This is the recommended regular expression to use. It matches all</span>
<span class="token comment">// emoji recommended for general interchange, as defined via the</span>
<span class="token comment">// `RGI_Emoji` property in the Unicode Standard.</span>
<span class="token comment">// https://unicode.org/reports/tr51/#def_rgi_set</span>
<span class="token comment">// When in doubt, use this!</span>
<span class="token keyword">const</span> emojiRegexRGI <span class="token operator">=</span> <span class="token function">require</span><span class="token punctuation">(</span><span class="token string">'emoji-regex/RGI_Emoji.js'</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

<span class="token comment">// This is the old regular expression, prior to `RGI_Emoji` being</span>
<span class="token comment">// standardized. In addition to all `RGI_Emoji` sequences, it matches</span>
<span class="token comment">// some emoji you probably don’t want to match (such as emoji component</span>
<span class="token comment">// symbols that are not meant to be used separately).</span>
<span class="token keyword">const</span> emojiRegex <span class="token operator">=</span> <span class="token function">require</span><span class="token punctuation">(</span><span class="token string">'emoji-regex/index.js'</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

<span class="token comment">// This regular expression matches even more emoji than the previous</span>
<span class="token comment">// one, including emoji that render as text instead of icons (i.e.</span>
<span class="token comment">// emoji that are not `Emoji_Presentation` symbols and that aren’t</span>
<span class="token comment">// forced to render as emoji by a variation selector).</span>
<span class="token keyword">const</span> emojiRegexText <span class="token operator">=</span> <span class="token function">require</span><span class="token punctuation">(</span><span class="token string">'emoji-regex/text.js'</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Additionally, in environments which support ES2015 Unicode escapes, you may <code v-pre>require</code> ES2015-style versions of the regexes:</p>
<div class="language-javascript ext-js line-numbers-mode"><pre v-pre class="language-javascript"><code><span class="token keyword">const</span> emojiRegexRGI <span class="token operator">=</span> <span class="token function">require</span><span class="token punctuation">(</span><span class="token string">'emoji-regex/es2015/RGI_Emoji.js'</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token keyword">const</span> emojiRegex <span class="token operator">=</span> <span class="token function">require</span><span class="token punctuation">(</span><span class="token string">'emoji-regex/es2015/index.js'</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token keyword">const</span> emojiRegexText <span class="token operator">=</span> <span class="token function">require</span><span class="token punctuation">(</span><span class="token string">'emoji-regex/es2015/text.js'</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="for-maintainers" tabindex="-1"><a class="header-anchor" href="#for-maintainers" aria-hidden="true">#</a> For maintainers</h2>
<h3 id="how-to-update-emoji-regex-after-new-unicode-standard-releases" tabindex="-1"><a class="header-anchor" href="#how-to-update-emoji-regex-after-new-unicode-standard-releases" aria-hidden="true">#</a> How to update emoji-regex after new Unicode Standard releases</h3>
<ol>
<li>
<p>Update the Unicode data dependency in <code v-pre>package.json</code> by running the following commands:</p>
<div class="language-bash ext-sh line-numbers-mode"><pre v-pre class="language-bash"><code><span class="token comment"># Example: updating from Unicode v12 to Unicode v13.</span>
<span class="token function">npm</span> uninstall @unicode/unicode-12.0.0
<span class="token function">npm</span> <span class="token function">install</span> @unicode/unicode-13.0.0 --save-dev
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></li>
<li>
<p>Generate the new output:</p>
<div class="language-bash ext-sh line-numbers-mode"><pre v-pre class="language-bash"><code><span class="token function">npm</span> run build
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div></li>
<li>
<p>Verify that tests still pass:</p>
<div class="language-bash ext-sh line-numbers-mode"><pre v-pre class="language-bash"><code><span class="token function">npm</span> <span class="token builtin class-name">test</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div></li>
<li>
<p>Send a pull request with the changes, and get it reviewed &amp; merged.</p>
</li>
<li>
<p>On the <code v-pre>main</code> branch, bump the emoji-regex version number in <code v-pre>package.json</code>:</p>
<div class="language-bash ext-sh line-numbers-mode"><pre v-pre class="language-bash"><code><span class="token function">npm</span> version patch -m <span class="token string">'Release v%s'</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Instead of <code v-pre>patch</code>, use <code v-pre>minor</code> or <code v-pre>major</code> <a href="https://semver.org/" target="_blank" rel="noopener noreferrer">as needed<ExternalLinkIcon/></a>.</p>
<p>Note that this produces a Git commit + tag.</p>
</li>
<li>
<p>Push the release commit and tag:</p>
<div class="language-bash ext-sh line-numbers-mode"><pre v-pre class="language-bash"><code><span class="token function">git</span> push
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Our CI then automatically publishes the new release to npm.</p>
</li>
</ol>
<h2 id="author" tabindex="-1"><a class="header-anchor" href="#author" aria-hidden="true">#</a> Author</h2>
<table>
<thead>
<tr>
<th><a href="https://twitter.com/mathias" title="Follow @mathias on Twitter" target="_blank" rel="noopener noreferrer"><img src="https://gravatar.com/avatar/24e08a9ea84deb17ae121074d0f17125?s=70" alt="twitter/mathias"><ExternalLinkIcon/></a></th>
</tr>
</thead>
<tbody>
<tr>
<td><a href="https://mathiasbynens.be/" target="_blank" rel="noopener noreferrer">Mathias Bynens<ExternalLinkIcon/></a></td>
</tr>
</tbody>
</table>
<h2 id="license" tabindex="-1"><a class="header-anchor" href="#license" aria-hidden="true">#</a> License</h2>
<p><u>emoji-regex</u> is available under the <a href="https://mths.be/mit" target="_blank" rel="noopener noreferrer">MIT<ExternalLinkIcon/></a> license.</p>
</div></template>
