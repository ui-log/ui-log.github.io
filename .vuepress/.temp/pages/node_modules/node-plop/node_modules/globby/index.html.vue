<template><div><h1 id="globby" tabindex="-1"><a class="header-anchor" href="#globby" aria-hidden="true">#</a> globby</h1>
<blockquote>
<p>User-friendly glob matching</p>
</blockquote>
<p>Based on <a href="https://github.com/mrmlnc/fast-glob" target="_blank" rel="noopener noreferrer"><code v-pre>fast-glob</code><ExternalLinkIcon/></a> but adds a bunch of useful features.</p>
<h2 id="features" tabindex="-1"><a class="header-anchor" href="#features" aria-hidden="true">#</a> Features</h2>
<ul>
<li>Promise API</li>
<li>Multiple patterns</li>
<li>Negated patterns: <code v-pre>['foo*', '!foobar']</code></li>
<li>Expands directories: <code v-pre>foo</code> → <code v-pre>foo/**/*</code></li>
<li>Supports <code v-pre>.gitignore</code> and similar ignore config files</li>
<li>Supports <code v-pre>URL</code> as <code v-pre>cwd</code></li>
</ul>
<h2 id="install" tabindex="-1"><a class="header-anchor" href="#install" aria-hidden="true">#</a> Install</h2>
<div class="language-text ext-text line-numbers-mode"><pre v-pre class="language-text"><code>$ npm install globby
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><h2 id="usage" tabindex="-1"><a class="header-anchor" href="#usage" aria-hidden="true">#</a> Usage</h2>
<div class="language-text ext-text line-numbers-mode"><pre v-pre class="language-text"><code>├── unicorn
├── cake
└── rainbow
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><div class="language-javascript ext-js line-numbers-mode"><pre v-pre class="language-javascript"><code><span class="token keyword">import</span> <span class="token punctuation">{</span>globby<span class="token punctuation">}</span> <span class="token keyword">from</span> <span class="token string">'globby'</span><span class="token punctuation">;</span>

<span class="token keyword">const</span> paths <span class="token operator">=</span> <span class="token keyword">await</span> <span class="token function">globby</span><span class="token punctuation">(</span><span class="token punctuation">[</span><span class="token string">'*'</span><span class="token punctuation">,</span> <span class="token string">'!cake'</span><span class="token punctuation">]</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span>paths<span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token comment">//=> ['unicorn', 'rainbow']</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="api" tabindex="-1"><a class="header-anchor" href="#api" aria-hidden="true">#</a> API</h2>
<p>Note that glob patterns can only contain forward-slashes, not backward-slashes, so if you want to construct a glob pattern from path components, you need to use <code v-pre>path.posix.join()</code> instead of <code v-pre>path.join()</code>.</p>
<h3 id="globby-patterns-options" tabindex="-1"><a class="header-anchor" href="#globby-patterns-options" aria-hidden="true">#</a> globby(patterns, options?)</h3>
<p>Returns a <code v-pre>Promise&lt;string[]&gt;</code> of matching paths.</p>
<h4 id="patterns" tabindex="-1"><a class="header-anchor" href="#patterns" aria-hidden="true">#</a> patterns</h4>
<p>Type: <code v-pre>string | string[]</code></p>
<p>See supported <code v-pre>minimatch</code> <a href="https://github.com/isaacs/minimatch#usage" target="_blank" rel="noopener noreferrer">patterns<ExternalLinkIcon/></a>.</p>
<h4 id="options" tabindex="-1"><a class="header-anchor" href="#options" aria-hidden="true">#</a> options</h4>
<p>Type: <code v-pre>object</code></p>
<p>See the <a href="https://github.com/mrmlnc/fast-glob#options-3" target="_blank" rel="noopener noreferrer"><code v-pre>fast-glob</code> options<ExternalLinkIcon/></a> in addition to the ones below.</p>
<h5 id="expanddirectories" tabindex="-1"><a class="header-anchor" href="#expanddirectories" aria-hidden="true">#</a> expandDirectories</h5>
<p>Type: <code v-pre>boolean | string[] | object</code><br>
Default: <code v-pre>true</code></p>
<p>If set to <code v-pre>true</code>, <code v-pre>globby</code> will automatically glob directories for you. If you define an <code v-pre>Array</code> it will only glob files that matches the patterns inside the <code v-pre>Array</code>. You can also define an <code v-pre>object</code> with <code v-pre>files</code> and <code v-pre>extensions</code> like below:</p>
<div class="language-javascript ext-js line-numbers-mode"><pre v-pre class="language-javascript"><code><span class="token keyword">import</span> <span class="token punctuation">{</span>globby<span class="token punctuation">}</span> <span class="token keyword">from</span> <span class="token string">'globby'</span><span class="token punctuation">;</span>

<span class="token punctuation">(</span><span class="token keyword">async</span> <span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">=></span> <span class="token punctuation">{</span>
	<span class="token keyword">const</span> paths <span class="token operator">=</span> <span class="token keyword">await</span> <span class="token function">globby</span><span class="token punctuation">(</span><span class="token string">'images'</span><span class="token punctuation">,</span> <span class="token punctuation">{</span>
		<span class="token literal-property property">expandDirectories</span><span class="token operator">:</span> <span class="token punctuation">{</span>
			<span class="token literal-property property">files</span><span class="token operator">:</span> <span class="token punctuation">[</span><span class="token string">'cat'</span><span class="token punctuation">,</span> <span class="token string">'unicorn'</span><span class="token punctuation">,</span> <span class="token string">'*.jpg'</span><span class="token punctuation">]</span><span class="token punctuation">,</span>
			<span class="token literal-property property">extensions</span><span class="token operator">:</span> <span class="token punctuation">[</span><span class="token string">'png'</span><span class="token punctuation">]</span>
		<span class="token punctuation">}</span>
	<span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

	console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span>paths<span class="token punctuation">)</span><span class="token punctuation">;</span>
	<span class="token comment">//=> ['cat.png', 'unicorn.png', 'cow.jpg', 'rainbow.jpg']</span>
<span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Note that if you set this option to <code v-pre>false</code>, you won't get back matched directories unless you set <code v-pre>onlyFiles: false</code>.</p>
<h5 id="gitignore" tabindex="-1"><a class="header-anchor" href="#gitignore" aria-hidden="true">#</a> gitignore</h5>
<p>Type: <code v-pre>boolean</code><br>
Default: <code v-pre>false</code></p>
<p>Respect ignore patterns in <code v-pre>.gitignore</code> files that apply to the globbed files.</p>
<h5 id="ignorefiles" tabindex="-1"><a class="header-anchor" href="#ignorefiles" aria-hidden="true">#</a> ignoreFiles</h5>
<p>Type: <code v-pre>string | string[]</code><br>
Default: <code v-pre>undefined</code></p>
<p>Glob patterns to look for ignore files, which are then used to ignore globbed files.</p>
<p>This is a more generic form of the <code v-pre>gitignore</code> option, allowing you to find ignore files with a <a href="http://git-scm.com/docs/gitignore" target="_blank" rel="noopener noreferrer">compatible syntax<ExternalLinkIcon/></a>. For instance, this works with Babel's <code v-pre>.babelignore</code>, Prettier's <code v-pre>.prettierignore</code>, or ESLint's <code v-pre>.eslintignore</code> files.</p>
<h3 id="globbysync-patterns-options" tabindex="-1"><a class="header-anchor" href="#globbysync-patterns-options" aria-hidden="true">#</a> globbySync(patterns, options?)</h3>
<p>Returns <code v-pre>string[]</code> of matching paths.</p>
<h3 id="globbystream-patterns-options" tabindex="-1"><a class="header-anchor" href="#globbystream-patterns-options" aria-hidden="true">#</a> globbyStream(patterns, options?)</h3>
<p>Returns a <a href="https://nodejs.org/api/stream.html#stream_readable_streams" target="_blank" rel="noopener noreferrer"><code v-pre>stream.Readable</code><ExternalLinkIcon/></a> of matching paths.</p>
<p>Since Node.js 10, <a href="https://nodejs.org/api/stream.html#stream_readable_symbol_asynciterator" target="_blank" rel="noopener noreferrer">readable streams are iterable<ExternalLinkIcon/></a>, so you can loop over glob matches in a <a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/for-await...of" target="_blank" rel="noopener noreferrer"><code v-pre>for await...of</code> loop<ExternalLinkIcon/></a> like this:</p>
<div class="language-javascript ext-js line-numbers-mode"><pre v-pre class="language-javascript"><code><span class="token keyword">import</span> <span class="token punctuation">{</span>globbyStream<span class="token punctuation">}</span> <span class="token keyword">from</span> <span class="token string">'globby'</span><span class="token punctuation">;</span>

<span class="token punctuation">(</span><span class="token keyword">async</span> <span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">=></span> <span class="token punctuation">{</span>
	<span class="token keyword">for</span> <span class="token keyword">await</span> <span class="token punctuation">(</span><span class="token keyword">const</span> path <span class="token keyword">of</span> <span class="token function">globbyStream</span><span class="token punctuation">(</span><span class="token string">'*.tmp'</span><span class="token punctuation">)</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
		console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span>path<span class="token punctuation">)</span><span class="token punctuation">;</span>
	<span class="token punctuation">}</span>
<span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h3 id="generateglobtasks-patterns-options" tabindex="-1"><a class="header-anchor" href="#generateglobtasks-patterns-options" aria-hidden="true">#</a> generateGlobTasks(patterns, options?)</h3>
<p>Returns an <code v-pre>Promise&lt;object[]&gt;</code> in the format <code v-pre>{patterns: string[], options: Object}</code>, which can be passed as arguments to <a href="https://github.com/mrmlnc/fast-glob" target="_blank" rel="noopener noreferrer"><code v-pre>fast-glob</code><ExternalLinkIcon/></a>. This is useful for other globbing-related packages.</p>
<p>Note that you should avoid running the same tasks multiple times as they contain a file system cache. Instead, run this method each time to ensure file system changes are taken into consideration.</p>
<h3 id="generateglobtaskssync-patterns-options" tabindex="-1"><a class="header-anchor" href="#generateglobtaskssync-patterns-options" aria-hidden="true">#</a> generateGlobTasksSync(patterns, options?)</h3>
<p>Returns an <code v-pre>object[]</code> in the format <code v-pre>{patterns: string[], options: Object}</code>, which can be passed as arguments to <a href="https://github.com/mrmlnc/fast-glob" target="_blank" rel="noopener noreferrer"><code v-pre>fast-glob</code><ExternalLinkIcon/></a>. This is useful for other globbing-related packages.</p>
<p>Takes the same arguments as <code v-pre>generateGlobTasks</code>.</p>
<h3 id="isdynamicpattern-patterns-options" tabindex="-1"><a class="header-anchor" href="#isdynamicpattern-patterns-options" aria-hidden="true">#</a> isDynamicPattern(patterns, options?)</h3>
<p>Returns a <code v-pre>boolean</code> of whether there are any special glob characters in the <code v-pre>patterns</code>.</p>
<p>Note that the options affect the results.</p>
<p>This function is backed by <a href="https://github.com/mrmlnc/fast-glob#isdynamicpatternpattern-options" target="_blank" rel="noopener noreferrer"><code v-pre>fast-glob</code><ExternalLinkIcon/></a>.</p>
<h3 id="isgitignored-options" tabindex="-1"><a class="header-anchor" href="#isgitignored-options" aria-hidden="true">#</a> isGitIgnored(options?)</h3>
<p>Returns a <code v-pre>Promise&lt;(path: URL | string) =&gt; boolean&gt;</code> indicating whether a given path is ignored via a <code v-pre>.gitignore</code> file.</p>
<p>Takes <code v-pre>cwd?: URL | string</code> as options.</p>
<div class="language-javascript ext-js line-numbers-mode"><pre v-pre class="language-javascript"><code><span class="token keyword">import</span> <span class="token punctuation">{</span>isGitIgnored<span class="token punctuation">}</span> <span class="token keyword">from</span> <span class="token string">'globby'</span><span class="token punctuation">;</span>

<span class="token keyword">const</span> isIgnored <span class="token operator">=</span> <span class="token keyword">await</span> <span class="token function">isGitIgnored</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span><span class="token function">isIgnored</span><span class="token punctuation">(</span><span class="token string">'some/file'</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h3 id="isgitignoredsync-options" tabindex="-1"><a class="header-anchor" href="#isgitignoredsync-options" aria-hidden="true">#</a> isGitIgnoredSync(options?)</h3>
<p>Returns a <code v-pre>(path: URL | string) =&gt; boolean</code> indicating whether a given path is ignored via a <code v-pre>.gitignore</code> file.</p>
<p>Takes <code v-pre>cwd?: URL | string</code> as options.</p>
<h2 id="globbing-patterns" tabindex="-1"><a class="header-anchor" href="#globbing-patterns" aria-hidden="true">#</a> Globbing patterns</h2>
<p>Just a quick overview.</p>
<ul>
<li><code v-pre>*</code> matches any number of characters, but not <code v-pre>/</code></li>
<li><code v-pre>?</code> matches a single character, but not <code v-pre>/</code></li>
<li><code v-pre>**</code> matches any number of characters, including <code v-pre>/</code>, as long as it's the only thing in a path part</li>
<li><code v-pre>{}</code> allows for a comma-separated list of &quot;or&quot; expressions</li>
<li><code v-pre>!</code> at the beginning of a pattern will negate the match</li>
</ul>
<p><a href="https://github.com/sindresorhus/multimatch/blob/main/test/test.js" target="_blank" rel="noopener noreferrer">Various patterns and expected matches.<ExternalLinkIcon/></a></p>
<h2 id="globby-for-enterprise" tabindex="-1"><a class="header-anchor" href="#globby-for-enterprise" aria-hidden="true">#</a> globby for enterprise</h2>
<p>Available as part of the Tidelift Subscription.</p>
<p>The maintainers of globby and thousands of other packages are working with Tidelift to deliver commercial support and maintenance for the open source dependencies you use to build your applications. Save time, reduce risk, and improve code health, while paying the maintainers of the exact dependencies you use. <a href="https://tidelift.com/subscription/pkg/npm-globby?utm_source=npm-globby&amp;utm_medium=referral&amp;utm_campaign=enterprise&amp;utm_term=repo" target="_blank" rel="noopener noreferrer">Learn more.<ExternalLinkIcon/></a></p>
<h2 id="related" tabindex="-1"><a class="header-anchor" href="#related" aria-hidden="true">#</a> Related</h2>
<ul>
<li><a href="https://github.com/sindresorhus/multimatch" target="_blank" rel="noopener noreferrer">multimatch<ExternalLinkIcon/></a> - Match against a list instead of the filesystem</li>
<li><a href="https://github.com/sindresorhus/matcher" target="_blank" rel="noopener noreferrer">matcher<ExternalLinkIcon/></a> - Simple wildcard matching</li>
<li><a href="https://github.com/sindresorhus/del" target="_blank" rel="noopener noreferrer">del<ExternalLinkIcon/></a> - Delete files and directories</li>
<li><a href="https://github.com/sindresorhus/make-dir" target="_blank" rel="noopener noreferrer">make-dir<ExternalLinkIcon/></a> - Make a directory and its parents if needed</li>
</ul>
</div></template>
