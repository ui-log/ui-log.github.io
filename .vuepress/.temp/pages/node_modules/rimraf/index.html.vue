<template><div><p><a href="https://travis-ci.org/isaacs/rimraf" target="_blank" rel="noopener noreferrer"><img src="https://travis-ci.org/isaacs/rimraf.svg?branch=master" alt="Build Status"><ExternalLinkIcon/></a> <a href="https://david-dm.org/isaacs/rimraf" target="_blank" rel="noopener noreferrer"><img src="https://david-dm.org/isaacs/rimraf.svg" alt="Dependency Status"><ExternalLinkIcon/></a> <a href="https://david-dm.org/isaacs/rimraf#info=devDependencies" target="_blank" rel="noopener noreferrer"><img src="https://david-dm.org/isaacs/rimraf/dev-status.svg" alt="devDependency Status"><ExternalLinkIcon/></a></p>
<p>The <a href="http://en.wikipedia.org/wiki/Rm_(Unix)" target="_blank" rel="noopener noreferrer">UNIX command<ExternalLinkIcon/></a> <code v-pre>rm -rf</code> for node.</p>
<p>Install with <code v-pre>npm install rimraf</code>, or just drop rimraf.js somewhere.</p>
<h2 id="api" tabindex="-1"><a class="header-anchor" href="#api" aria-hidden="true">#</a> API</h2>
<p><code v-pre>rimraf(f, [opts], callback)</code></p>
<p>The first parameter will be interpreted as a globbing pattern for files. If you
want to disable globbing you can do so with <code v-pre>opts.disableGlob</code> (defaults to
<code v-pre>false</code>). This might be handy, for instance, if you have filenames that contain
globbing wildcard characters.</p>
<p>The callback will be called with an error if there is one.  Certain
errors are handled for you:</p>
<ul>
<li>Windows: <code v-pre>EBUSY</code> and <code v-pre>ENOTEMPTY</code> - rimraf will back off a maximum of
<code v-pre>opts.maxBusyTries</code> times before giving up, adding 100ms of wait
between each attempt.  The default <code v-pre>maxBusyTries</code> is 3.</li>
<li><code v-pre>ENOENT</code> - If the file doesn't exist, rimraf will return
successfully, since your desired outcome is already the case.</li>
<li><code v-pre>EMFILE</code> - Since <code v-pre>readdir</code> requires opening a file descriptor, it's
possible to hit <code v-pre>EMFILE</code> if too many file descriptors are in use.
In the sync case, there's nothing to be done for this.  But in the
async case, rimraf will gradually back off with timeouts up to
<code v-pre>opts.emfileWait</code> ms, which defaults to 1000.</li>
</ul>
<h2 id="options" tabindex="-1"><a class="header-anchor" href="#options" aria-hidden="true">#</a> options</h2>
<ul>
<li>
<p>unlink, chmod, stat, lstat, rmdir, readdir,
unlinkSync, chmodSync, statSync, lstatSync, rmdirSync, readdirSync</p>
<p>In order to use a custom file system library, you can override
specific fs functions on the options object.</p>
<p>If any of these functions are present on the options object, then
the supplied function will be used instead of the default fs
method.</p>
<p>Sync methods are only relevant for <code v-pre>rimraf.sync()</code>, of course.</p>
<p>For example:</p>
<div class="language-javascript ext-js line-numbers-mode"><pre v-pre class="language-javascript"><code><span class="token keyword">var</span> myCustomFS <span class="token operator">=</span> <span class="token function">require</span><span class="token punctuation">(</span><span class="token string">'some-custom-fs'</span><span class="token punctuation">)</span>

<span class="token function">rimraf</span><span class="token punctuation">(</span><span class="token string">'some-thing'</span><span class="token punctuation">,</span> myCustomFS<span class="token punctuation">,</span> callback<span class="token punctuation">)</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></li>
<li>
<p>maxBusyTries</p>
<p>If an <code v-pre>EBUSY</code>, <code v-pre>ENOTEMPTY</code>, or <code v-pre>EPERM</code> error code is encountered
on Windows systems, then rimraf will retry with a linear backoff
wait of 100ms longer on each try.  The default maxBusyTries is 3.</p>
<p>Only relevant for async usage.</p>
</li>
<li>
<p>emfileWait</p>
<p>If an <code v-pre>EMFILE</code> error is encountered, then rimraf will retry
repeatedly with a linear backoff of 1ms longer on each try, until
the timeout counter hits this max.  The default limit is 1000.</p>
<p>If you repeatedly encounter <code v-pre>EMFILE</code> errors, then consider using
<a href="http://npm.im/graceful-fs" target="_blank" rel="noopener noreferrer">graceful-fs<ExternalLinkIcon/></a> in your program.</p>
<p>Only relevant for async usage.</p>
</li>
<li>
<p>glob</p>
<p>Set to <code v-pre>false</code> to disable <a href="http://npm.im/glob" target="_blank" rel="noopener noreferrer">glob<ExternalLinkIcon/></a> pattern
matching.</p>
<p>Set to an object to pass options to the glob module.  The default
glob options are <code v-pre>{ nosort: true, silent: true }</code>.</p>
<p>Glob version 6 is used in this module.</p>
<p>Relevant for both sync and async usage.</p>
</li>
<li>
<p>disableGlob</p>
<p>Set to any non-falsey value to disable globbing entirely.
(Equivalent to setting <code v-pre>glob: false</code>.)</p>
</li>
</ul>
<h2 id="rimraf-sync" tabindex="-1"><a class="header-anchor" href="#rimraf-sync" aria-hidden="true">#</a> rimraf.sync</h2>
<p>It can remove stuff synchronously, too.  But that's not so good.  Use
the async API.  It's better.</p>
<h2 id="cli" tabindex="-1"><a class="header-anchor" href="#cli" aria-hidden="true">#</a> CLI</h2>
<p>If installed with <code v-pre>npm install rimraf -g</code> it can be used as a global
command <code v-pre>rimraf &lt;path&gt; [&lt;path&gt; ...]</code> which is useful for cross platform support.</p>
<h2 id="mkdirp" tabindex="-1"><a class="header-anchor" href="#mkdirp" aria-hidden="true">#</a> mkdirp</h2>
<p>If you need to create a directory recursively, check out
<a href="https://github.com/substack/node-mkdirp" target="_blank" rel="noopener noreferrer">mkdirp<ExternalLinkIcon/></a>.</p>
</div></template>
