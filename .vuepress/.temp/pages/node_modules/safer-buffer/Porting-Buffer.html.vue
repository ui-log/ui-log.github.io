<template><div><h1 id="porting-to-the-buffer-from-buffer-alloc-api" tabindex="-1"><a class="header-anchor" href="#porting-to-the-buffer-from-buffer-alloc-api" aria-hidden="true">#</a> Porting to the Buffer.from/Buffer.alloc API</h1>
<p><a id="overview"></a></p>
<h2 id="overview" tabindex="-1"><a class="header-anchor" href="#overview" aria-hidden="true">#</a> Overview</h2>
<ul>
<li><a href="#variant-1">Variant 1: Drop support for Node.js ≤ 4.4.x and 5.0.0 — 5.9.x.</a> (<em>recommended</em>)</li>
<li><a href="#variant-2">Variant 2: Use a polyfill</a></li>
<li><a href="#variant-3">Variant 3: manual detection, with safeguards</a></li>
</ul>
<h3 id="finding-problematic-bits-of-code-using-grep" tabindex="-1"><a class="header-anchor" href="#finding-problematic-bits-of-code-using-grep" aria-hidden="true">#</a> Finding problematic bits of code using grep</h3>
<p>Just run <code v-pre>grep -nrE '[^a-zA-Z](Slow)?Buffer\s*\(' --exclude-dir node_modules</code>.</p>
<p>It will find all the potentially unsafe places in your own code (with some considerably unlikely
exceptions).</p>
<h3 id="finding-problematic-bits-of-code-using-node-js-8" tabindex="-1"><a class="header-anchor" href="#finding-problematic-bits-of-code-using-node-js-8" aria-hidden="true">#</a> Finding problematic bits of code using Node.js 8</h3>
<p>If you’re using Node.js ≥ 8.0.0 (which is recommended), Node.js exposes multiple options that help with finding the relevant pieces of code:</p>
<ul>
<li><code v-pre>--trace-warnings</code> will make Node.js show a stack trace for this warning and other warnings that are printed by Node.js.</li>
<li><code v-pre>--trace-deprecation</code> does the same thing, but only for deprecation warnings.</li>
<li><code v-pre>--pending-deprecation</code> will show more types of deprecation warnings. In particular, it will show the <code v-pre>Buffer()</code> deprecation warning, even on Node.js 8.</li>
</ul>
<p>You can set these flags using an environment variable:</p>
<div class="language-console ext-console line-numbers-mode"><pre v-pre class="language-console"><code>$ export NODE_OPTIONS='--trace-warnings --pending-deprecation'
$ cat example.js
'use strict';
const foo = new Buffer('foo');
$ node example.js
(node:7147) [DEP0005] DeprecationWarning: The Buffer() and new Buffer() constructors are not recommended for use due to security and usability concerns. Please use the new Buffer.alloc(), Buffer.allocUnsafe(), or Buffer.from() construction methods instead.
    at showFlaggedDeprecation (buffer.js:127:13)
    at new Buffer (buffer.js:148:3)
    at Object.&lt;anonymous&gt; (/path/to/example.js:2:13)
    [... more stack trace lines ...]
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h3 id="finding-problematic-bits-of-code-using-linters" tabindex="-1"><a class="header-anchor" href="#finding-problematic-bits-of-code-using-linters" aria-hidden="true">#</a> Finding problematic bits of code using linters</h3>
<p>Eslint rules <a href="https://eslint.org/docs/rules/no-buffer-constructor" target="_blank" rel="noopener noreferrer">no-buffer-constructor<ExternalLinkIcon/></a>
or
<a href="https://github.com/mysticatea/eslint-plugin-node/blob/master/docs/rules/no-deprecated-api.md" target="_blank" rel="noopener noreferrer">node/no-deprecated-api<ExternalLinkIcon/></a>
also find calls to deprecated <code v-pre>Buffer()</code> API. Those rules are included in some pre-sets.</p>
<p>There is a drawback, though, that it doesn't always
<a href="https://github.com/chalker/safer-buffer#why-not-safe-buffer" target="_blank" rel="noopener noreferrer">work correctly<ExternalLinkIcon/></a> when <code v-pre>Buffer</code> is
overriden e.g. with a polyfill, so recommended is a combination of this and some other method
described above.</p>
<p><a id="variant-1"></a></p>
<h2 id="variant-1-drop-support-for-node-js-≤-4-4-x-and-5-0-0-—-5-9-x" tabindex="-1"><a class="header-anchor" href="#variant-1-drop-support-for-node-js-≤-4-4-x-and-5-0-0-—-5-9-x" aria-hidden="true">#</a> Variant 1: Drop support for Node.js ≤ 4.4.x and 5.0.0 — 5.9.x.</h2>
<p>This is the recommended solution nowadays that would imply only minimal overhead.</p>
<p>The Node.js 5.x release line has been unsupported since July 2016, and the Node.js 4.x release line reaches its End of Life in April 2018 (→ <a href="https://github.com/nodejs/Release#release-schedule" target="_blank" rel="noopener noreferrer">Schedule<ExternalLinkIcon/></a>). This means that these versions of Node.js will <em>not</em> receive any updates, even in case of security issues, so using these release lines should be avoided, if at all possible.</p>
<p>What you would do in this case is to convert all <code v-pre>new Buffer()</code> or <code v-pre>Buffer()</code> calls to use <code v-pre>Buffer.alloc()</code> or <code v-pre>Buffer.from()</code>, in the following way:</p>
<ul>
<li>For <code v-pre>new Buffer(number)</code>, replace it with <code v-pre>Buffer.alloc(number)</code>.</li>
<li>For <code v-pre>new Buffer(string)</code> (or <code v-pre>new Buffer(string, encoding)</code>), replace it with <code v-pre>Buffer.from(string)</code> (or <code v-pre>Buffer.from(string, encoding)</code>).</li>
<li>For all other combinations of arguments (these are much rarer), also replace <code v-pre>new Buffer(...arguments)</code> with <code v-pre>Buffer.from(...arguments)</code>.</li>
</ul>
<p>Note that <code v-pre>Buffer.alloc()</code> is also <u>faster</u> on the current Node.js versions than
<code v-pre>new Buffer(size).fill(0)</code>, which is what you would otherwise need to ensure zero-filling.</p>
<p>Enabling eslint rule <a href="https://eslint.org/docs/rules/no-buffer-constructor" target="_blank" rel="noopener noreferrer">no-buffer-constructor<ExternalLinkIcon/></a>
or
<a href="https://github.com/mysticatea/eslint-plugin-node/blob/master/docs/rules/no-deprecated-api.md" target="_blank" rel="noopener noreferrer">node/no-deprecated-api<ExternalLinkIcon/></a>
is recommended to avoid accidential unsafe Buffer API usage.</p>
<p>There is also a <a href="https://github.com/joyeecheung/node-dep-codemod#dep005" target="_blank" rel="noopener noreferrer">JSCodeshift codemod<ExternalLinkIcon/></a>
for automatically migrating Buffer constructors to <code v-pre>Buffer.alloc()</code> or <code v-pre>Buffer.from()</code>.
Note that it currently only works with cases where the arguments are literals or where the
constructor is invoked with two arguments.</p>
<p><u>If you currently support those older Node.js versions and dropping them would be a semver-major change
for you, or if you support older branches of your packages, consider using <a href="#variant-2">Variant 2</a>
or <a href="#variant-3">Variant 3</a> on older branches, so people using those older branches will also receive
the fix. That way, you will eradicate potential issues caused by unguarded Buffer API usage and
your users will not observe a runtime deprecation warning when running your code on Node.js 10.</u></p>
<p><a id="variant-2"></a></p>
<h2 id="variant-2-use-a-polyfill" tabindex="-1"><a class="header-anchor" href="#variant-2-use-a-polyfill" aria-hidden="true">#</a> Variant 2: Use a polyfill</h2>
<p>Utilize <a href="https://www.npmjs.com/package/safer-buffer" target="_blank" rel="noopener noreferrer">safer-buffer<ExternalLinkIcon/></a> as a polyfill to support older
Node.js versions.</p>
<p>You would take exacly the same steps as in <a href="#variant-1">Variant 1</a>, but with a polyfill
<code v-pre>const Buffer = require('safer-buffer').Buffer</code> in all files where you use the new <code v-pre>Buffer</code> api.</p>
<p>Make sure that you do not use old <code v-pre>new Buffer</code> API — in any files where the line above is added,
using old <code v-pre>new Buffer()</code> API will <u>throw</u>. It will be easy to notice that in CI, though.</p>
<p>Alternatively, you could use <a href="https://www.npmjs.com/package/buffer-from" target="_blank" rel="noopener noreferrer">buffer-from<ExternalLinkIcon/></a> and/or
<a href="https://www.npmjs.com/package/buffer-alloc" target="_blank" rel="noopener noreferrer">buffer-alloc<ExternalLinkIcon/></a> <a href="https://ponyfill.com/" target="_blank" rel="noopener noreferrer">ponyfills<ExternalLinkIcon/></a> —
those are great, the only downsides being 4 deps in the tree and slightly more code changes to
migrate off them (as you would be using e.g. <code v-pre>Buffer.from</code> under a different name). If you need only
<code v-pre>Buffer.from</code> polyfilled — <code v-pre>buffer-from</code> alone which comes with no extra dependencies.</p>
<p><u>Alternatively, you could use <a href="https://www.npmjs.com/package/safe-buffer" target="_blank" rel="noopener noreferrer">safe-buffer<ExternalLinkIcon/></a> — it also
provides a polyfill, but takes a different approach which has
<a href="https://github.com/chalker/safer-buffer#why-not-safe-buffer" target="_blank" rel="noopener noreferrer">it's drawbacks<ExternalLinkIcon/></a>. It will allow you
to also use the older <code v-pre>new Buffer()</code> API in your code, though — but that's arguably a benefit, as
it is problematic, can cause issues in your code, and will start emitting runtime deprecation
warnings starting with Node.js 10.</u></p>
<p>Note that in either case, it is important that you also remove all calls to the old Buffer
API manually — just throwing in <code v-pre>safe-buffer</code> doesn't fix the problem by itself, it just provides
a polyfill for the new API. I have seen people doing that mistake.</p>
<p>Enabling eslint rule <a href="https://eslint.org/docs/rules/no-buffer-constructor" target="_blank" rel="noopener noreferrer">no-buffer-constructor<ExternalLinkIcon/></a>
or
<a href="https://github.com/mysticatea/eslint-plugin-node/blob/master/docs/rules/no-deprecated-api.md" target="_blank" rel="noopener noreferrer">node/no-deprecated-api<ExternalLinkIcon/></a>
is recommended.</p>
<p><u>Don't forget to drop the polyfill usage once you drop support for Node.js &lt; 4.5.0.</u></p>
<p><a id="variant-3"></a></p>
<h2 id="variant-3-—-manual-detection-with-safeguards" tabindex="-1"><a class="header-anchor" href="#variant-3-—-manual-detection-with-safeguards" aria-hidden="true">#</a> Variant 3 — manual detection, with safeguards</h2>
<p>This is useful if you create Buffer instances in only a few places (e.g. one), or you have your own
wrapper around them.</p>
<h3 id="buffer-0" tabindex="-1"><a class="header-anchor" href="#buffer-0" aria-hidden="true">#</a> Buffer(0)</h3>
<p>This special case for creating empty buffers can be safely replaced with <code v-pre>Buffer.concat([])</code>, which
returns the same result all the way down to Node.js 0.8.x.</p>
<h3 id="buffer-notnumber" tabindex="-1"><a class="header-anchor" href="#buffer-notnumber" aria-hidden="true">#</a> Buffer(notNumber)</h3>
<p>Before:</p>
<div class="language-javascript ext-js line-numbers-mode"><pre v-pre class="language-javascript"><code><span class="token keyword">var</span> buf <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">Buffer</span><span class="token punctuation">(</span>notNumber<span class="token punctuation">,</span> encoding<span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>After:</p>
<div class="language-javascript ext-js line-numbers-mode"><pre v-pre class="language-javascript"><code><span class="token keyword">var</span> buf<span class="token punctuation">;</span>
<span class="token keyword">if</span> <span class="token punctuation">(</span>Buffer<span class="token punctuation">.</span>from <span class="token operator">&amp;&amp;</span> Buffer<span class="token punctuation">.</span>from <span class="token operator">!==</span> Uint8Array<span class="token punctuation">.</span>from<span class="token punctuation">)</span> <span class="token punctuation">{</span>
  buf <span class="token operator">=</span> Buffer<span class="token punctuation">.</span><span class="token function">from</span><span class="token punctuation">(</span>notNumber<span class="token punctuation">,</span> encoding<span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span> <span class="token keyword">else</span> <span class="token punctuation">{</span>
  <span class="token keyword">if</span> <span class="token punctuation">(</span><span class="token keyword">typeof</span> notNumber <span class="token operator">===</span> <span class="token string">'number'</span><span class="token punctuation">)</span>
    <span class="token keyword">throw</span> <span class="token keyword">new</span> <span class="token class-name">Error</span><span class="token punctuation">(</span><span class="token string">'The "size" argument must be of type number.'</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
  buf <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">Buffer</span><span class="token punctuation">(</span>notNumber<span class="token punctuation">,</span> encoding<span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code v-pre>encoding</code> is optional.</p>
<p>Note that the <code v-pre>typeof notNumber</code> before <code v-pre>new Buffer</code> is required (for cases when <code v-pre>notNumber</code> argument is not
hard-coded) and <u>is not caused by the deprecation of Buffer constructor</u> — it's exactly <u>why</u> the
Buffer constructor is deprecated. Ecosystem packages lacking this type-check caused numereous
security issues — situations when unsanitized user input could end up in the <code v-pre>Buffer(arg)</code> create
problems ranging from DoS to leaking sensitive information to the attacker from the process memory.</p>
<p>When <code v-pre>notNumber</code> argument is hardcoded (e.g. literal <code v-pre>&quot;abc&quot;</code> or <code v-pre>[0,1,2]</code>), the <code v-pre>typeof</code> check can
be omitted.</p>
<p>Also note that using TypeScript does not fix this problem for you — when libs written in
<code v-pre>TypeScript</code> are used from JS, or when user input ends up there — it behaves exactly as pure JS, as
all type checks are translation-time only and are not present in the actual JS code which TS
compiles to.</p>
<h3 id="buffer-number" tabindex="-1"><a class="header-anchor" href="#buffer-number" aria-hidden="true">#</a> Buffer(number)</h3>
<p>For Node.js 0.10.x (and below) support:</p>
<div class="language-javascript ext-js line-numbers-mode"><pre v-pre class="language-javascript"><code><span class="token keyword">var</span> buf<span class="token punctuation">;</span>
<span class="token keyword">if</span> <span class="token punctuation">(</span>Buffer<span class="token punctuation">.</span>alloc<span class="token punctuation">)</span> <span class="token punctuation">{</span>
  buf <span class="token operator">=</span> Buffer<span class="token punctuation">.</span><span class="token function">alloc</span><span class="token punctuation">(</span>number<span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span> <span class="token keyword">else</span> <span class="token punctuation">{</span>
  buf <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">Buffer</span><span class="token punctuation">(</span>number<span class="token punctuation">)</span><span class="token punctuation">;</span>
  buf<span class="token punctuation">.</span><span class="token function">fill</span><span class="token punctuation">(</span><span class="token number">0</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Otherwise (Node.js ≥ 0.12.x):</p>
<div class="language-javascript ext-js line-numbers-mode"><pre v-pre class="language-javascript"><code><span class="token keyword">const</span> buf <span class="token operator">=</span> Buffer<span class="token punctuation">.</span>alloc <span class="token operator">?</span> Buffer<span class="token punctuation">.</span><span class="token function">alloc</span><span class="token punctuation">(</span>number<span class="token punctuation">)</span> <span class="token operator">:</span> <span class="token keyword">new</span> <span class="token class-name">Buffer</span><span class="token punctuation">(</span>number<span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">fill</span><span class="token punctuation">(</span><span class="token number">0</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><h2 id="regarding-buffer-allocunsafe" tabindex="-1"><a class="header-anchor" href="#regarding-buffer-allocunsafe" aria-hidden="true">#</a> Regarding Buffer.allocUnsafe</h2>
<p>Be extra cautious when using <code v-pre>Buffer.allocUnsafe</code>:</p>
<ul>
<li>Don't use it if you don't have a good reason to
<ul>
<li>e.g. you probably won't ever see a performance difference for small buffers, in fact, those
might be even faster with <code v-pre>Buffer.alloc()</code>,</li>
<li>if your code is not in the hot code path — you also probably won't notice a difference,</li>
<li>keep in mind that zero-filling minimizes the potential risks.</li>
</ul>
</li>
<li>If you use it, make sure that you never return the buffer in a partially-filled state,
<ul>
<li>if you are writing to it sequentially — always truncate it to the actuall written length</li>
</ul>
</li>
</ul>
<p>Errors in handling buffers allocated with <code v-pre>Buffer.allocUnsafe</code> could result in various issues,
ranged from undefined behaviour of your code to sensitive data (user input, passwords, certs)
leaking to the remote attacker.</p>
<p><u>Note that the same applies to <code v-pre>new Buffer</code> usage without zero-filling, depending on the Node.js
version (and lacking type checks also adds DoS to the list of potential problems).</u></p>
<p><a id="faq"></a></p>
<h2 id="faq" tabindex="-1"><a class="header-anchor" href="#faq" aria-hidden="true">#</a> FAQ</h2>
<p><a id="design-flaws"></a></p>
<h3 id="what-is-wrong-with-the-buffer-constructor" tabindex="-1"><a class="header-anchor" href="#what-is-wrong-with-the-buffer-constructor" aria-hidden="true">#</a> What is wrong with the <code v-pre>Buffer</code> constructor?</h3>
<p>The <code v-pre>Buffer</code> constructor could be used to create a buffer in many different ways:</p>
<ul>
<li><code v-pre>new Buffer(42)</code> creates a <code v-pre>Buffer</code> of 42 bytes. Before Node.js 8, this buffer contained
<em>arbitrary memory</em> for performance reasons, which could include anything ranging from
program source code to passwords and encryption keys.</li>
<li><code v-pre>new Buffer('abc')</code> creates a <code v-pre>Buffer</code> that contains the UTF-8-encoded version of
the string <code v-pre>'abc'</code>. A second argument could specify another encoding: For example,
<code v-pre>new Buffer(string, 'base64')</code> could be used to convert a Base64 string into the original
sequence of bytes that it represents.</li>
<li>There are several other combinations of arguments.</li>
</ul>
<p>This meant that, in code like <code v-pre>var buffer = new Buffer(foo);</code>, <em>it is not possible to tell
what exactly the contents of the generated buffer are</em> without knowing the type of <code v-pre>foo</code>.</p>
<p>Sometimes, the value of <code v-pre>foo</code> comes from an external source. For example, this function
could be exposed as a service on a web server, converting a UTF-8 string into its Base64 form:</p>
<div class="language-text ext-text line-numbers-mode"><pre v-pre class="language-text"><code>function stringToBase64(req, res) {
  // The request body should have the format of `{ string: 'foobar' }`
  const rawBytes = new Buffer(req.body.string)
  const encoded = rawBytes.toString('base64')
  res.end({ encoded: encoded })
}
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Note that this code does <em>not</em> validate the type of <code v-pre>req.body.string</code>:</p>
<ul>
<li><code v-pre>req.body.string</code> is expected to be a string. If this is the case, all goes well.</li>
<li><code v-pre>req.body.string</code> is controlled by the client that sends the request.</li>
<li>If <code v-pre>req.body.string</code> is the <em>number</em> <code v-pre>50</code>, the <code v-pre>rawBytes</code> would be 50 bytes:
<ul>
<li>Before Node.js 8, the content would be uninitialized</li>
<li>After Node.js 8, the content would be <code v-pre>50</code> bytes with the value <code v-pre>0</code></li>
</ul>
</li>
</ul>
<p>Because of the missing type check, an attacker could intentionally send a number
as part of the request. Using this, they can either:</p>
<ul>
<li>Read uninitialized memory. This <strong>will</strong> leak passwords, encryption keys and other
kinds of sensitive information. (Information leak)</li>
<li>Force the program to allocate a large amount of memory. For example, when specifying
<code v-pre>500000000</code> as the input value, each request will allocate 500MB of memory.
This can be used to either exhaust the memory available of a program completely
and make it crash, or slow it down significantly. (Denial of Service)</li>
</ul>
<p>Both of these scenarios are considered serious security issues in a real-world
web server context.</p>
<p>when using <code v-pre>Buffer.from(req.body.string)</code> instead, passing a number will always
throw an exception instead, giving a controlled behaviour that can always be
handled by the program.</p>
<p><a id="ecosystem-usage"></a></p>
<h3 id="the-buffer-constructor-has-been-deprecated-for-a-while-is-this-really-an-issue" tabindex="-1"><a class="header-anchor" href="#the-buffer-constructor-has-been-deprecated-for-a-while-is-this-really-an-issue" aria-hidden="true">#</a> The <code v-pre>Buffer()</code> constructor has been deprecated for a while. Is this really an issue?</h3>
<p>Surveys of code in the <code v-pre>npm</code> ecosystem have shown that the <code v-pre>Buffer()</code> constructor is still
widely used. This includes new code, and overall usage of such code has actually been
<em>increasing</em>.</p>
</div></template>
