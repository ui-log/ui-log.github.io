<template><div><!--
BEFORE EDITING THIS README
Our README.md is auto-generated by combining pages in website/docs and website/readme-sources

If you are sending a pull request to improve documentation, submit your changes
in the source markdown files and we will generate the README from there.

You can build the readme with this command:

    cd website && yarn build-readme
-->
<h1 id="" tabindex="-1"><a class="header-anchor" href="#" aria-hidden="true">#</a> <a href="https://typestrong.org/ts-node" target="_blank" rel="noopener noreferrer"><img src="logo.svg?sanitize=true" alt="TypeScript Node"><ExternalLinkIcon/></a></h1>
<p><a href="https://npmjs.org/package/ts-node" target="_blank" rel="noopener noreferrer"><img src="https://img.shields.io/npm/v/ts-node.svg?style=flat" alt="NPM version"><ExternalLinkIcon/></a>
<a href="https://npmjs.org/package/ts-node" target="_blank" rel="noopener noreferrer"><img src="https://img.shields.io/npm/dm/ts-node.svg?style=flat" alt="NPM downloads"><ExternalLinkIcon/></a>
<a href="https://github.com/TypeStrong/ts-node/actions?query=workflow%3A%22Continuous+Integration%22" target="_blank" rel="noopener noreferrer"><img src="https://img.shields.io/github/workflow/status/TypeStrong/ts-node/Continuous Integration" alt="Build status"><ExternalLinkIcon/></a>
<a href="https://codecov.io/gh/TypeStrong/ts-node" target="_blank" rel="noopener noreferrer"><img src="https://codecov.io/gh/TypeStrong/ts-node/branch/main/graph/badge.svg" alt="Test coverage"><ExternalLinkIcon/></a></p>
<blockquote>
<p>TypeScript execution and REPL for node.js, with source map and native ESM support.</p>
</blockquote>
<p>The latest documentation can also be found on our website: <a href="https://typestrong.org/ts-node" target="_blank" rel="noopener noreferrer">https://typestrong.org/ts-node<ExternalLinkIcon/></a></p>
<h1 id="table-of-contents" tabindex="-1"><a class="header-anchor" href="#table-of-contents" aria-hidden="true">#</a> Table of Contents</h1>
<ul>
<li><a href="#overview">Overview</a>
<ul>
<li><a href="#features">Features</a></li>
</ul>
</li>
<li><a href="#installation">Installation</a></li>
<li><a href="#usage">Usage</a>
<ul>
<li><a href="#command-line">Command Line</a></li>
<li><a href="#shebang">Shebang</a></li>
<li><a href="#node-flags-and-other-tools">node flags and other tools</a></li>
<li><a href="#programmatic">Programmatic</a></li>
</ul>
</li>
<li><a href="#configuration">Configuration</a>
<ul>
<li><a href="#cli-flags">CLI flags</a></li>
<li><a href="#via-tsconfigjson-recommended">Via tsconfig.json (recommended)</a>
<ul>
<li><a href="#tsconfigbases">@tsconfig/bases</a></li>
<li><a href="#default-config">Default config</a></li>
</ul>
</li>
<li><a href="#node-flags"><code v-pre>node</code> flags</a></li>
</ul>
</li>
<li><a href="#options">Options</a>
<ul>
<li><a href="#cli-options">CLI Options</a>
<ul>
<li><a href="#help">help</a></li>
<li><a href="#version">version</a></li>
<li><a href="#eval">eval</a></li>
<li><a href="#print">print</a></li>
<li><a href="#interactive">interactive</a></li>
<li><a href="#esm">esm</a></li>
</ul>
</li>
<li><a href="#tsconfig-options">TSConfig Options</a>
<ul>
<li><a href="#project">project</a></li>
<li><a href="#skipproject">skipProject</a></li>
<li><a href="#cwdmode">cwdMode</a></li>
<li><a href="#compileroptions">compilerOptions</a></li>
<li><a href="#showconfig">showConfig</a></li>
</ul>
</li>
<li><a href="#typechecking">Typechecking</a>
<ul>
<li><a href="#transpileonly">transpileOnly</a></li>
<li><a href="#typecheck">typeCheck</a></li>
<li><a href="#compilerhost">compilerHost</a></li>
<li><a href="#files">files</a></li>
<li><a href="#ignorediagnostics">ignoreDiagnostics</a></li>
</ul>
</li>
<li><a href="#transpilation-options">Transpilation Options</a>
<ul>
<li><a href="#ignore">ignore</a></li>
<li><a href="#skipignore">skipIgnore</a></li>
<li><a href="#compiler">compiler</a></li>
<li><a href="#swc">swc</a></li>
<li><a href="#transpiler">transpiler</a></li>
<li><a href="#prefertsexts">preferTsExts</a></li>
</ul>
</li>
<li><a href="#diagnostic-options">Diagnostic Options</a>
<ul>
<li><a href="#logerror">logError</a></li>
<li><a href="#pretty">pretty</a></li>
<li><a href="#ts_node_debug">TS_NODE_DEBUG</a></li>
</ul>
</li>
<li><a href="#advanced-options">Advanced Options</a>
<ul>
<li><a href="#require">require</a></li>
<li><a href="#cwd">cwd</a></li>
<li><a href="#emit">emit</a></li>
<li><a href="#scope">scope</a></li>
<li><a href="#scopedir">scopeDir</a></li>
<li><a href="#moduletypes">moduleTypes</a></li>
<li><a href="#ts_node_history">TS_NODE_HISTORY</a></li>
<li><a href="#noexperimentalreplawait">noExperimentalReplAwait</a></li>
<li><a href="#experimentalresolver">experimentalResolver</a></li>
<li><a href="#experimentalspecifierresolution">experimentalSpecifierResolution</a></li>
</ul>
</li>
<li><a href="#api-options">API Options</a></li>
</ul>
</li>
<li><a href="#swc-1">SWC</a></li>
<li><a href="#commonjs-vs-native-ecmascript-modules">CommonJS vs native ECMAScript modules</a>
<ul>
<li><a href="#commonjs">CommonJS</a></li>
<li><a href="#native-ecmascript-modules">Native ECMAScript modules</a></li>
</ul>
</li>
<li><a href="#troubleshooting">Troubleshooting</a>
<ul>
<li><a href="#configuration-1">Configuration</a></li>
<li><a href="#common-errors">Common errors</a>
<ul>
<li><a href="#tserror"><code v-pre>TSError</code></a></li>
<li><a href="#syntaxerror"><code v-pre>SyntaxError</code></a>
<ul>
<li><a href="#unsupported-javascript-syntax">Unsupported JavaScript syntax</a></li>
</ul>
</li>
<li><a href="#err_require_esm"><code v-pre>ERR_REQUIRE_ESM</code></a></li>
<li><a href="#err_unknown_file_extension"><code v-pre>ERR_UNKNOWN_FILE_EXTENSION</code></a></li>
</ul>
</li>
<li><a href="#missing-types">Missing Types</a></li>
<li><a href="#npx-yarn-dlx-and-node_modules">npx, yarn dlx, and node_modules</a></li>
</ul>
</li>
<li><a href="#performance">Performance</a>
<ul>
<li><a href="#skip-typechecking">Skip typechecking</a></li>
<li><a href="#with-typechecking">With typechecking</a></li>
</ul>
</li>
<li><a href="#advanced">Advanced</a>
<ul>
<li><a href="#how-it-works">How it works</a></li>
<li><a href="#ignored-files">Ignored files</a>
<ul>
<li><a href="#file-extensions">File extensions</a></li>
<li><a href="#skipping-node_modules">Skipping <code v-pre>node_modules</code></a></li>
<li><a href="#skipping-pre-compiled-typescript">Skipping pre-compiled TypeScript</a></li>
<li><a href="#scope-by-directory">Scope by directory</a></li>
<li><a href="#ignore-by-regexp">Ignore by regexp</a></li>
</ul>
</li>
<li><a href="#paths-and-baseurl">paths and baseUrl
</a>
<ul>
<li><a href="#why-is-this-not-built-in-to-ts-node">Why is this not built-in to ts-node?</a></li>
</ul>
</li>
<li><a href="#third-party-compilers">Third-party compilers</a></li>
<li><a href="#transpilers">Transpilers</a>
<ul>
<li><a href="#third-party-plugins">Third-party plugins</a></li>
<li><a href="#write-your-own-plugin">Write your own plugin</a></li>
</ul>
</li>
<li><a href="#module-type-overrides">Module type overrides</a>
<ul>
<li><a href="#caveats">Caveats</a></li>
</ul>
</li>
<li><a href="#api">API</a></li>
</ul>
</li>
<li><a href="#recipes">Recipes</a>
<ul>
<li><a href="#watching-and-restarting">Watching and restarting</a></li>
<li><a href="#ava">AVA</a>
<ul>
<li><a href="#commonjs-1">CommonJS</a></li>
<li><a href="#native-ecmascript-modules-1">Native ECMAScript modules</a></li>
</ul>
</li>
<li><a href="#gulp">Gulp</a></li>
<li><a href="#intellij-and-webstorm">IntelliJ and Webstorm</a></li>
<li><a href="#mocha">Mocha</a>
<ul>
<li><a href="#mocha-7-and-newer">Mocha 7 and newer</a></li>
<li><a href="#mocha-6">Mocha &lt;=6</a></li>
</ul>
</li>
<li><a href="#tape">Tape</a></li>
<li><a href="#visual-studio-code">Visual Studio Code</a></li>
<li><a href="#other">Other</a></li>
</ul>
</li>
<li><a href="#license">License</a></li>
</ul>
<h1 id="overview" tabindex="-1"><a class="header-anchor" href="#overview" aria-hidden="true">#</a> Overview</h1>
<p>ts-node is a TypeScript execution engine and REPL for Node.js.</p>
<p>It JIT transforms TypeScript into JavaScript, enabling you to directly execute TypeScript on Node.js without precompiling.
This is accomplished by hooking node's module loading APIs, enabling it to be used seamlessly alongside other Node.js
tools and libraries.</p>
<h2 id="features" tabindex="-1"><a class="header-anchor" href="#features" aria-hidden="true">#</a> Features</h2>
<ul>
<li>Automatic sourcemaps in stack traces</li>
<li>Automatic <code v-pre>tsconfig.json</code> parsing</li>
<li>Automatic defaults to match your node version</li>
<li>Typechecking (optional)</li>
<li>REPL</li>
<li>Write standalone scripts</li>
<li>Native ESM loader</li>
<li>Use third-party transpilers</li>
<li>Use custom transformers</li>
<li>Integrate with test runners, debuggers, and CLI tools</li>
<li>Compatible with pre-compilation for production</li>
</ul>
<p><img src="website/static/img/screenshot.png" alt="TypeScript REPL"></p>
<h1 id="installation" tabindex="-1"><a class="header-anchor" href="#installation" aria-hidden="true">#</a> Installation</h1>
<div class="language-bash ext-sh line-numbers-mode"><pre v-pre class="language-bash"><code><span class="token comment"># Locally in your project.</span>
<span class="token function">npm</span> <span class="token function">install</span> -D typescript
<span class="token function">npm</span> <span class="token function">install</span> -D ts-node

<span class="token comment"># Or globally with TypeScript.</span>
<span class="token function">npm</span> <span class="token function">install</span> -g typescript
<span class="token function">npm</span> <span class="token function">install</span> -g ts-node

<span class="token comment"># Depending on configuration, you may also need these</span>
<span class="token function">npm</span> <span class="token function">install</span> -D tslib @types/node
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><strong>Tip:</strong> Installing modules locally allows you to control and share the versions through <code v-pre>package.json</code>. ts-node will always resolve the compiler from <code v-pre>cwd</code> before checking relative to its own installation.</p>
<h1 id="usage" tabindex="-1"><a class="header-anchor" href="#usage" aria-hidden="true">#</a> Usage</h1>
<h2 id="command-line" tabindex="-1"><a class="header-anchor" href="#command-line" aria-hidden="true">#</a> Command Line</h2>
<div class="language-bash ext-sh line-numbers-mode"><pre v-pre class="language-bash"><code><span class="token comment"># Execute a script as `node` + `tsc`.</span>
ts-node script.ts

<span class="token comment"># Starts a TypeScript REPL.</span>
ts-node

<span class="token comment"># Execute code with TypeScript.</span>
ts-node -e <span class="token string">'console.log("Hello, world!")'</span>

<span class="token comment"># Execute, and print, code with TypeScript.</span>
ts-node -p -e <span class="token string">'"Hello, world!"'</span>

<span class="token comment"># Pipe scripts to execute with TypeScript.</span>
<span class="token builtin class-name">echo</span> <span class="token string">'console.log("Hello, world!")'</span> <span class="token operator">|</span> ts-node

<span class="token comment"># Equivalent to ts-node --transpileOnly</span>
ts-node-transpile-only script.ts

<span class="token comment"># Equivalent to ts-node --cwdMode</span>
ts-node-cwd script.ts

<span class="token comment"># Equivalent to ts-node --esm</span>
ts-node-esm script.ts
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="shebang" tabindex="-1"><a class="header-anchor" href="#shebang" aria-hidden="true">#</a> Shebang</h2>
<p>To write scripts with maximum portability, <a href="#via-tsconfigjson-recommended">specify options in your <code v-pre>tsconfig.json</code></a> and omit them from the shebang.</p>
<div class="language-typescript ext-ts line-numbers-mode"><pre v-pre class="language-typescript"><code><span class="token hashbang comment">#!/usr/bin/env ts-node</span>

<span class="token comment">// ts-node options are read from tsconfig.json</span>

<span class="token builtin">console</span><span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span><span class="token string">"Hello, world!"</span><span class="token punctuation">)</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Including options within the shebang requires the <a href="https://manpages.debian.org/bullseye/coreutils/env.1.en.html#S" target="_blank" rel="noopener noreferrer"><code v-pre>env -S</code> flag<ExternalLinkIcon/></a>, which is available on recent versions of <code v-pre>env</code>. (<a href="https://github.com/TypeStrong/ts-node/pull/1448#issuecomment-913895766" target="_blank" rel="noopener noreferrer">compatibility<ExternalLinkIcon/></a>)</p>
<div class="language-typescript ext-ts line-numbers-mode"><pre v-pre class="language-typescript"><code><span class="token hashbang comment">#!/usr/bin/env -S ts-node --files</span>
<span class="token comment">// This shebang works on Mac and Linux with newer versions of env</span>
<span class="token comment">// Technically, Mac allows omitting `-S`, but Linux requires it</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>To test your version of <code v-pre>env</code> for compatibility with <code v-pre>-S</code>:</p>
<div class="language-bash ext-sh line-numbers-mode"><pre v-pre class="language-bash"><code><span class="token comment"># Note that these unusual quotes are necessary</span>
/usr/bin/env --debug <span class="token string">'-S echo foo bar'</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="node-flags-and-other-tools" tabindex="-1"><a class="header-anchor" href="#node-flags-and-other-tools" aria-hidden="true">#</a> node flags and other tools</h2>
<p>You can register ts-node without using our CLI: <code v-pre>node -r ts-node/register</code> and <code v-pre>node --loader ts-node/esm</code></p>
<p>In many cases, setting <a href="https://nodejs.org/api/cli.html#cli_node_options_options" target="_blank" rel="noopener noreferrer"><code v-pre>NODE_OPTIONS</code><ExternalLinkIcon/></a> will enable <code v-pre>ts-node</code> within other node tools, child processes, and worker threads.  This can be combined with other node flags.</p>
<div class="language-bash ext-sh line-numbers-mode"><pre v-pre class="language-bash"><code><span class="token assign-left variable">NODE_OPTIONS</span><span class="token operator">=</span><span class="token string">"-r ts-node/register --no-warnings"</span> <span class="token function">node</span> ./index.ts
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Or, if you require native ESM support:</p>
<div class="language-bash ext-sh line-numbers-mode"><pre v-pre class="language-bash"><code><span class="token assign-left variable">NODE_OPTIONS</span><span class="token operator">=</span><span class="token string">"--loader ts-node/esm"</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>This tells any node processes which receive this environment variable to install <code v-pre>ts-node</code>'s hooks before executing other code.</p>
<p>If you are invoking node directly, you can avoid the environment variable and pass those flags to node.</p>
<div class="language-bash ext-sh line-numbers-mode"><pre v-pre class="language-bash"><code><span class="token function">node</span> --loader ts-node/esm --inspect ./index.ts
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><h2 id="programmatic" tabindex="-1"><a class="header-anchor" href="#programmatic" aria-hidden="true">#</a> Programmatic</h2>
<p>You can require ts-node and register the loader for future requires by using <code v-pre>require('ts-node').register({ /* options */ })</code>.</p>
<p>Check out our <a href="#api">API</a> for more features.</p>
<h1 id="configuration" tabindex="-1"><a class="header-anchor" href="#configuration" aria-hidden="true">#</a> Configuration</h1>
<p>ts-node supports a variety of options which can be specified via <code v-pre>tsconfig.json</code>, as CLI flags, as environment variables, or programmatically.</p>
<p>For a complete list, see <a href="#options">Options</a>.</p>
<h2 id="cli-flags" tabindex="-1"><a class="header-anchor" href="#cli-flags" aria-hidden="true">#</a> CLI flags</h2>
<p>ts-node CLI flags must come <em>before</em> the entrypoint script. For example:</p>
<div class="language-bash ext-sh line-numbers-mode"><pre v-pre class="language-bash"><code>$ ts-node --project tsconfig-dev.json say-hello.ts Ronald
Hello, Ronald<span class="token operator">!</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="via-tsconfig-json-recommended" tabindex="-1"><a class="header-anchor" href="#via-tsconfig-json-recommended" aria-hidden="true">#</a> Via tsconfig.json (recommended)</h2>
<p>ts-node automatically finds and loads <code v-pre>tsconfig.json</code>.  Most ts-node options can be specified in a <code v-pre>&quot;ts-node&quot;</code> object using their programmatic, camelCase names. We recommend this because it works even when you cannot pass CLI flags, such as <code v-pre>node --require ts-node/register</code> and when using shebangs.</p>
<p>Use <code v-pre>--skipProject</code> to skip loading the <code v-pre>tsconfig.json</code>.  Use <code v-pre>--project</code> to explicitly specify the path to a <code v-pre>tsconfig.json</code>.</p>
<p>When searching, it is resolved using <a href="https://www.typescriptlang.org/docs/handbook/tsconfig-json.html" target="_blank" rel="noopener noreferrer">the same search behavior as <code v-pre>tsc</code><ExternalLinkIcon/></a>. By default, this search is performed relative to the entrypoint script. In <code v-pre>--cwdMode</code> or if no entrypoint is specified -- for example when using the REPL -- the search is performed relative to <code v-pre>--cwd</code> / <code v-pre>process.cwd()</code>.</p>
<p>You can use this sample configuration as a starting point:</p>
<div class="language-jsonc ext-jsonc line-numbers-mode"><pre v-pre class="language-jsonc"><code>{
  // This is an alias to @tsconfig/node16: https://github.com/tsconfig/bases
  &quot;extends&quot;: &quot;ts-node/node16/tsconfig.json&quot;,

  // Most ts-node options can be specified here using their programmatic names.
  &quot;ts-node&quot;: {
    // It is faster to skip typechecking.
    // Remove if you want ts-node to do typechecking.
    &quot;transpileOnly&quot;: true,

    &quot;files&quot;: true,

    &quot;compilerOptions&quot;: {
      // compilerOptions specified here will override those declared below,
      // but *only* in ts-node.  Useful if you want ts-node and tsc to use
      // different options with a single tsconfig.json.
    }
  },
  &quot;compilerOptions&quot;: {
    // typescript options here
  }
}
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Our bundled <a href="https://unpkg.com/browse/ts-node@latest/tsconfig.schema.json" target="_blank" rel="noopener noreferrer">JSON schema<ExternalLinkIcon/></a> lists all compatible options.</p>
<h3 id="tsconfig-bases" tabindex="-1"><a class="header-anchor" href="#tsconfig-bases" aria-hidden="true">#</a> @tsconfig/bases</h3>
<p><a href="https://github.com/tsconfig/bases" target="_blank" rel="noopener noreferrer">@tsconfig/bases<ExternalLinkIcon/></a> maintains recommended configurations for several node versions.
As a convenience, these are bundled with ts-node.</p>
<div class="language-jsonc ext-jsonc line-numbers-mode"><pre v-pre class="language-jsonc"><code>{
  &quot;extends&quot;: &quot;ts-node/node16/tsconfig.json&quot;,

  // Or install directly with `npm i -D @tsconfig/node16`
  &quot;extends&quot;: &quot;@tsconfig/node16/tsconfig.json&quot;,
}
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h3 id="default-config" tabindex="-1"><a class="header-anchor" href="#default-config" aria-hidden="true">#</a> Default config</h3>
<p>If no <code v-pre>tsconfig.json</code> is loaded from disk, ts-node will use the newest recommended defaults from
<a href="https://github.com/tsconfig/bases/" target="_blank" rel="noopener noreferrer">@tsconfig/bases<ExternalLinkIcon/></a> compatible with your <code v-pre>node</code> and <code v-pre>typescript</code> versions.
With the latest <code v-pre>node</code> and <code v-pre>typescript</code>, this is <a href="https://github.com/tsconfig/bases/blob/master/bases/node16.json" target="_blank" rel="noopener noreferrer"><code v-pre>@tsconfig/node16</code><ExternalLinkIcon/></a>.</p>
<p>Older versions of <code v-pre>typescript</code> are incompatible with <code v-pre>@tsconfig/node16</code>.  In those cases we will use an older default configuration.</p>
<p>When in doubt, <code v-pre>ts-node --showConfig</code> will log the configuration being used, and <code v-pre>ts-node -vv</code> will log <code v-pre>node</code> and <code v-pre>typescript</code> versions.</p>
<h2 id="node-flags" tabindex="-1"><a class="header-anchor" href="#node-flags" aria-hidden="true">#</a> <code v-pre>node</code> flags</h2>
<p><a href="https://nodejs.org/api/cli.html" target="_blank" rel="noopener noreferrer"><code v-pre>node</code> flags<ExternalLinkIcon/></a> must be passed directly to <code v-pre>node</code>; they cannot be passed to the ts-node binary nor can they be specified in <code v-pre>tsconfig.json</code></p>
<p>We recommend using the <a href="https://nodejs.org/api/cli.html#cli_node_options_options" target="_blank" rel="noopener noreferrer"><code v-pre>NODE_OPTIONS</code><ExternalLinkIcon/></a> environment variable to pass options to <code v-pre>node</code>.</p>
<div class="language-bash ext-sh line-numbers-mode"><pre v-pre class="language-bash"><code><span class="token assign-left variable">NODE_OPTIONS</span><span class="token operator">=</span><span class="token string">'--trace-deprecation --abort-on-uncaught-exception'</span> ts-node ./index.ts
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Alternatively, you can invoke <code v-pre>node</code> directly and install ts-node via <code v-pre>--require</code>/<code v-pre>-r</code></p>
<div class="language-bash ext-sh line-numbers-mode"><pre v-pre class="language-bash"><code><span class="token function">node</span> --trace-deprecation --abort-on-uncaught-exception -r ts-node/register ./index.ts
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><h1 id="options" tabindex="-1"><a class="header-anchor" href="#options" aria-hidden="true">#</a> Options</h1>
<p>All command-line flags support both <code v-pre>--camelCase</code> and <code v-pre>--hyphen-case</code>.</p>
<p>Most options can be declared in your tsconfig.json: <a href="#via-tsconfigjson-recommended">Configuration via tsconfig.json</a></p>
<p><code v-pre>ts-node</code> supports <code v-pre>--print</code> (<code v-pre>-p</code>), <code v-pre>--eval</code> (<code v-pre>-e</code>), <code v-pre>--require</code> (<code v-pre>-r</code>) and <code v-pre>--interactive</code> (<code v-pre>-i</code>) similar to the <a href="https://nodejs.org/api/cli.html" target="_blank" rel="noopener noreferrer">node.js CLI<ExternalLinkIcon/></a>.</p>
<p><code v-pre>ts-node</code> supports <code v-pre>--project</code> and <code v-pre>--showConfig</code> similar to the <a href="https://www.typescriptlang.org/docs/handbook/compiler-options.html#compiler-options" target="_blank" rel="noopener noreferrer">tsc CLI<ExternalLinkIcon/></a>.</p>
<p><em>Environment variables, where available, are in <code v-pre>ALL_CAPS</code></em></p>
<h2 id="cli-options" tabindex="-1"><a class="header-anchor" href="#cli-options" aria-hidden="true">#</a> CLI Options</h2>
<h3 id="help" tabindex="-1"><a class="header-anchor" href="#help" aria-hidden="true">#</a> help</h3>
<div class="language-bash ext-sh line-numbers-mode"><pre v-pre class="language-bash"><code>ts-node --help
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Prints the help text</p>
<h3 id="version" tabindex="-1"><a class="header-anchor" href="#version" aria-hidden="true">#</a> version</h3>
<div class="language-bash ext-sh line-numbers-mode"><pre v-pre class="language-bash"><code>ts-node -v
ts-node -vvv
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>Prints the version. <code v-pre>-vv</code> includes node and typescript compiler versions.  <code v-pre>-vvv</code> includes absolute paths to ts-node and
typescript installations.</p>
<h3 id="eval" tabindex="-1"><a class="header-anchor" href="#eval" aria-hidden="true">#</a> eval</h3>
<div class="language-bash ext-sh line-numbers-mode"><pre v-pre class="language-bash"><code>ts-node -e <span class="token operator">&lt;</span>typescript code<span class="token operator">></span>
<span class="token comment"># Example</span>
ts-node -e <span class="token string">'console.log("Hello world!")'</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Evaluate code</p>
<h3 id="print" tabindex="-1"><a class="header-anchor" href="#print" aria-hidden="true">#</a> print</h3>
<div class="language-bash ext-sh line-numbers-mode"><pre v-pre class="language-bash"><code>ts-node -p -e <span class="token operator">&lt;</span>typescript code<span class="token operator">></span>
<span class="token comment"># Example</span>
ts-node -p -e <span class="token string">'"Hello world!"'</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Print result of <code v-pre>--eval</code></p>
<h3 id="interactive" tabindex="-1"><a class="header-anchor" href="#interactive" aria-hidden="true">#</a> interactive</h3>
<div class="language-bash ext-sh line-numbers-mode"><pre v-pre class="language-bash"><code>ts-node -i
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Opens the REPL even if stdin does not appear to be a terminal</p>
<h3 id="esm" tabindex="-1"><a class="header-anchor" href="#esm" aria-hidden="true">#</a> esm</h3>
<div class="language-bash ext-sh line-numbers-mode"><pre v-pre class="language-bash"><code>ts-node --esm
ts-node-esm
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>Bootstrap with the ESM loader, enabling full ESM support</p>
<h2 id="tsconfig-options" tabindex="-1"><a class="header-anchor" href="#tsconfig-options" aria-hidden="true">#</a> TSConfig Options</h2>
<h3 id="project" tabindex="-1"><a class="header-anchor" href="#project" aria-hidden="true">#</a> project</h3>
<div class="language-bash ext-sh line-numbers-mode"><pre v-pre class="language-bash"><code>ts-node -P <span class="token operator">&lt;</span>path/to/tsconfig<span class="token operator">></span>
ts-node --project <span class="token operator">&lt;</span>path/to/tsconfig<span class="token operator">></span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>Path to tsconfig file.</p>
<p><em>Note the uppercase <code v-pre>-P</code>. This is different from <code v-pre>tsc</code>'s <code v-pre>-p/--project</code> option.</em></p>
<p><em>Environment:</em> <code v-pre>TS_NODE_PROJECT</code></p>
<h3 id="skipproject" tabindex="-1"><a class="header-anchor" href="#skipproject" aria-hidden="true">#</a> skipProject</h3>
<div class="language-bash ext-sh line-numbers-mode"><pre v-pre class="language-bash"><code>ts-node --skipProject
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Skip project config resolution and loading</p>
<p><em>Default:</em> <code v-pre>false</code> <br/>
<em>Environment:</em> <code v-pre>TS_NODE_SKIP_PROJECT</code></p>
<h3 id="cwdmode" tabindex="-1"><a class="header-anchor" href="#cwdmode" aria-hidden="true">#</a> cwdMode</h3>
<div class="language-bash ext-sh line-numbers-mode"><pre v-pre class="language-bash"><code>ts-node -c
ts-node --cwdMode
ts-node-cwd
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Resolve config relative to the current directory instead of the directory of the entrypoint script</p>
<h3 id="compileroptions" tabindex="-1"><a class="header-anchor" href="#compileroptions" aria-hidden="true">#</a> compilerOptions</h3>
<div class="language-bash ext-sh line-numbers-mode"><pre v-pre class="language-bash"><code>ts-node -O <span class="token operator">&lt;</span>json compilerOptions<span class="token operator">></span>
ts-node --compilerOptions <span class="token operator">&lt;</span>json compilerOptions<span class="token operator">></span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>JSON object to merge with compiler options</p>
<p><em>Environment:</em> <code v-pre>TS_NODE_COMPILER_OPTIONS</code></p>
<h3 id="showconfig" tabindex="-1"><a class="header-anchor" href="#showconfig" aria-hidden="true">#</a> showConfig</h3>
<div class="language-bash ext-sh line-numbers-mode"><pre v-pre class="language-bash"><code>ts-node --showConfig
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Print resolved <code v-pre>tsconfig.json</code>, including <code v-pre>ts-node</code> options, and exit</p>
<h2 id="typechecking" tabindex="-1"><a class="header-anchor" href="#typechecking" aria-hidden="true">#</a> Typechecking</h2>
<h3 id="transpileonly" tabindex="-1"><a class="header-anchor" href="#transpileonly" aria-hidden="true">#</a> transpileOnly</h3>
<div class="language-bash ext-sh line-numbers-mode"><pre v-pre class="language-bash"><code>ts-node -T
ts-node --transpileOnly
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>Use TypeScript's faster <code v-pre>transpileModule</code></p>
<p><em>Default:</em> <code v-pre>false</code><br/>
<em>Environment:</em> <code v-pre>TS_NODE_TRANSPILE_ONLY</code></p>
<h3 id="typecheck" tabindex="-1"><a class="header-anchor" href="#typecheck" aria-hidden="true">#</a> typeCheck</h3>
<div class="language-bash ext-sh line-numbers-mode"><pre v-pre class="language-bash"><code>ts-node --typeCheck
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Opposite of <code v-pre>--transpileOnly</code></p>
<p><em>Default:</em> <code v-pre>true</code><br/>
<em>Environment:</em> <code v-pre>TS_NODE_TYPE_CHECK</code></p>
<h3 id="compilerhost" tabindex="-1"><a class="header-anchor" href="#compilerhost" aria-hidden="true">#</a> compilerHost</h3>
<div class="language-bash ext-sh line-numbers-mode"><pre v-pre class="language-bash"><code>ts-node -H
ts-node --compilerHost
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>Use TypeScript's compiler host API</p>
<p><em>Default:</em> <code v-pre>false</code> <br/>
<em>Environment:</em> <code v-pre>TS_NODE_COMPILER_HOST</code></p>
<h3 id="files" tabindex="-1"><a class="header-anchor" href="#files" aria-hidden="true">#</a> files</h3>
<div class="language-bash ext-sh line-numbers-mode"><pre v-pre class="language-bash"><code>ts-node --files
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Load <code v-pre>files</code>, <code v-pre>include</code> and <code v-pre>exclude</code> from <code v-pre>tsconfig.json</code> on startup.  This may
avoid certain typechecking failures.  See <a href="#missing-types">Missing types</a> for details.</p>
<p><em>Default:</em> <code v-pre>false</code> <br/>
<em>Environment:</em> <code v-pre>TS_NODE_FILES</code></p>
<h3 id="ignorediagnostics" tabindex="-1"><a class="header-anchor" href="#ignorediagnostics" aria-hidden="true">#</a> ignoreDiagnostics</h3>
<div class="language-bash ext-sh line-numbers-mode"><pre v-pre class="language-bash"><code>ts-node -D <span class="token operator">&lt;</span>code,code<span class="token operator">></span>
ts-node --ignoreDiagnostics <span class="token operator">&lt;</span>code,code<span class="token operator">></span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>Ignore TypeScript warnings by diagnostic code</p>
<p><em>Environment:</em> <code v-pre>TS_NODE_IGNORE_DIAGNOSTICS</code></p>
<h2 id="transpilation-options" tabindex="-1"><a class="header-anchor" href="#transpilation-options" aria-hidden="true">#</a> Transpilation Options</h2>
<h3 id="ignore" tabindex="-1"><a class="header-anchor" href="#ignore" aria-hidden="true">#</a> ignore</h3>
<div class="language-bash ext-sh line-numbers-mode"><pre v-pre class="language-bash"><code>ts-node -I <span class="token operator">&lt;</span>regexp matching ignored files<span class="token operator">></span>
ts-node --ignore <span class="token operator">&lt;</span>regexp matching ignored files<span class="token operator">></span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>Override the path patterns to skip compilation</p>
<p><em>Default:</em> <code v-pre>/node_modules/</code> <br/>
<em>Environment:</em> <code v-pre>TS_NODE_IGNORE</code></p>
<h3 id="skipignore" tabindex="-1"><a class="header-anchor" href="#skipignore" aria-hidden="true">#</a> skipIgnore</h3>
<div class="language-bash ext-sh line-numbers-mode"><pre v-pre class="language-bash"><code>ts-node --skipIgnore
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Skip ignore checks</p>
<p><em>Default:</em> <code v-pre>false</code> <br/>
<em>Environment:</em> <code v-pre>TS_NODE_SKIP_IGNORE</code></p>
<h3 id="compiler" tabindex="-1"><a class="header-anchor" href="#compiler" aria-hidden="true">#</a> compiler</h3>
<div class="language-bash ext-sh line-numbers-mode"><pre v-pre class="language-bash"><code>ts-node -C <span class="token operator">&lt;</span>name<span class="token operator">></span>
ts-node --compiler <span class="token operator">&lt;</span>name<span class="token operator">></span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>Specify a custom TypeScript compiler</p>
<p><em>Default:</em> <code v-pre>typescript</code> <br/>
<em>Environment:</em> <code v-pre>TS_NODE_COMPILER</code></p>
<h3 id="swc" tabindex="-1"><a class="header-anchor" href="#swc" aria-hidden="true">#</a> swc</h3>
<div class="language-bash ext-sh line-numbers-mode"><pre v-pre class="language-bash"><code>ts-node --swc
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Transpile with <a href="#swc">swc</a>.  Implies <code v-pre>--transpileOnly</code></p>
<p><em>Default:</em> <code v-pre>false</code></p>
<h3 id="transpiler" tabindex="-1"><a class="header-anchor" href="#transpiler" aria-hidden="true">#</a> transpiler</h3>
<div class="language-bash ext-sh line-numbers-mode"><pre v-pre class="language-bash"><code>ts-node --transpiler <span class="token operator">&lt;</span>name<span class="token operator">></span>
<span class="token comment"># Example</span>
ts-node --transpiler ts-node/transpilers/swc
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Use a third-party, non-typechecking transpiler</p>
<h3 id="prefertsexts" tabindex="-1"><a class="header-anchor" href="#prefertsexts" aria-hidden="true">#</a> preferTsExts</h3>
<div class="language-bash ext-sh line-numbers-mode"><pre v-pre class="language-bash"><code>ts-node --preferTsExts
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Re-order file extensions so that TypeScript imports are preferred</p>
<p><em>Default:</em> <code v-pre>false</code> <br/>
<em>Environment:</em> <code v-pre>TS_NODE_PREFER_TS_EXTS</code></p>
<h2 id="diagnostic-options" tabindex="-1"><a class="header-anchor" href="#diagnostic-options" aria-hidden="true">#</a> Diagnostic Options</h2>
<h3 id="logerror" tabindex="-1"><a class="header-anchor" href="#logerror" aria-hidden="true">#</a> logError</h3>
<div class="language-bash ext-sh line-numbers-mode"><pre v-pre class="language-bash"><code>ts-node --logError
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Logs TypeScript errors to stderr instead of throwing exceptions</p>
<p><em>Default:</em> <code v-pre>false</code> <br/>
<em>Environment:</em> <code v-pre>TS_NODE_LOG_ERROR</code></p>
<h3 id="pretty" tabindex="-1"><a class="header-anchor" href="#pretty" aria-hidden="true">#</a> pretty</h3>
<div class="language-bash ext-sh line-numbers-mode"><pre v-pre class="language-bash"><code>ts-node --pretty
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Use pretty diagnostic formatter</p>
<p><em>Default:</em> <code v-pre>false</code> <br/>
<em>Environment:</em> <code v-pre>TS_NODE_PRETTY</code></p>
<h3 id="ts-node-debug" tabindex="-1"><a class="header-anchor" href="#ts-node-debug" aria-hidden="true">#</a> TS_NODE_DEBUG</h3>
<div class="language-bash ext-sh line-numbers-mode"><pre v-pre class="language-bash"><code><span class="token assign-left variable">TS_NODE_DEBUG</span><span class="token operator">=</span>true ts-node
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Enable debug logging</p>
<h2 id="advanced-options" tabindex="-1"><a class="header-anchor" href="#advanced-options" aria-hidden="true">#</a> Advanced Options</h2>
<h3 id="require" tabindex="-1"><a class="header-anchor" href="#require" aria-hidden="true">#</a> require</h3>
<div class="language-bash ext-sh line-numbers-mode"><pre v-pre class="language-bash"><code>ts-node -r <span class="token operator">&lt;</span>module name or path<span class="token operator">></span>
ts-node --require <span class="token operator">&lt;</span>module name or path<span class="token operator">></span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>Require a node module before execution</p>
<h3 id="cwd" tabindex="-1"><a class="header-anchor" href="#cwd" aria-hidden="true">#</a> cwd</h3>
<div class="language-bash ext-sh line-numbers-mode"><pre v-pre class="language-bash"><code>ts-node --cwd <span class="token operator">&lt;</span>path/to/directory<span class="token operator">></span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Behave as if invoked in this working directory</p>
<p><em>Default:</em> <code v-pre>process.cwd()</code><br/>
<em>Environment:</em> <code v-pre>TS_NODE_CWD</code></p>
<h3 id="emit" tabindex="-1"><a class="header-anchor" href="#emit" aria-hidden="true">#</a> emit</h3>
<div class="language-bash ext-sh line-numbers-mode"><pre v-pre class="language-bash"><code>ts-node --emit
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Emit output files into <code v-pre>.ts-node</code> directory. Requires <code v-pre>--compilerHost</code></p>
<p><em>Default:</em> <code v-pre>false</code> <br/>
<em>Environment:</em> <code v-pre>TS_NODE_EMIT</code></p>
<h3 id="scope" tabindex="-1"><a class="header-anchor" href="#scope" aria-hidden="true">#</a> scope</h3>
<div class="language-bash ext-sh line-numbers-mode"><pre v-pre class="language-bash"><code>ts-node --scope
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Scope compiler to files within <code v-pre>scopeDir</code>.  Anything outside this directory is ignored.</p>
<p><em>Default:</em> <code v-pre>false</code> <br/>
<em>Environment:</em> <code v-pre>TS_NODE_SCOPE</code></p>
<h3 id="scopedir" tabindex="-1"><a class="header-anchor" href="#scopedir" aria-hidden="true">#</a> scopeDir</h3>
<div class="language-bash ext-sh line-numbers-mode"><pre v-pre class="language-bash"><code>ts-node --scopeDir <span class="token operator">&lt;</span>path/to/directory<span class="token operator">></span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Directory within which compiler is limited when <code v-pre>scope</code> is enabled.</p>
<p><em>Default:</em> First of: <code v-pre>tsconfig.json</code> &quot;rootDir&quot; if specified, directory containing <code v-pre>tsconfig.json</code>, or cwd if no <code v-pre>tsconfig.json</code> is loaded.<br/>
<em>Environment:</em> <code v-pre>TS_NODE_SCOPE_DIR</code></p>
<h3 id="moduletypes" tabindex="-1"><a class="header-anchor" href="#moduletypes" aria-hidden="true">#</a> moduleTypes</h3>
<p>Override the module type of certain files, ignoring the <code v-pre>package.json</code> <code v-pre>&quot;type&quot;</code> field.  See <a href="#module-type-overrides">Module type overrides</a> for details.</p>
<p><em>Default:</em> obeys <code v-pre>package.json</code> <code v-pre>&quot;type&quot;</code> and <code v-pre>tsconfig.json</code> <code v-pre>&quot;module&quot;</code> <br/>
<em>Can only be specified via <code v-pre>tsconfig.json</code> or API.</em></p>
<h3 id="ts-node-history" tabindex="-1"><a class="header-anchor" href="#ts-node-history" aria-hidden="true">#</a> TS_NODE_HISTORY</h3>
<div class="language-bash ext-sh line-numbers-mode"><pre v-pre class="language-bash"><code><span class="token assign-left variable">TS_NODE_HISTORY</span><span class="token operator">=</span><span class="token operator">&lt;</span>path/to/history/file<span class="token operator">></span> ts-node
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Path to history file for REPL</p>
<p><em>Default:</em> <code v-pre>~/.ts_node_repl_history</code></p>
<h3 id="noexperimentalreplawait" tabindex="-1"><a class="header-anchor" href="#noexperimentalreplawait" aria-hidden="true">#</a> noExperimentalReplAwait</h3>
<div class="language-bash ext-sh line-numbers-mode"><pre v-pre class="language-bash"><code>ts-node --noExperimentalReplAwait
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Disable top-level await in REPL.  Equivalent to node's <a href="https://nodejs.org/api/cli.html#cli_no_experimental_repl_await" target="_blank" rel="noopener noreferrer"><code v-pre>--no-experimental-repl-await</code><ExternalLinkIcon/></a></p>
<p><em>Default:</em> Enabled if TypeScript version is 3.8 or higher and target is ES2018 or higher.<br/>
<em>Environment:</em> <code v-pre>TS_NODE_EXPERIMENTAL_REPL_AWAIT</code> set <code v-pre>false</code> to disable</p>
<h3 id="experimentalresolver" tabindex="-1"><a class="header-anchor" href="#experimentalresolver" aria-hidden="true">#</a> experimentalResolver</h3>
<p>Enable experimental hooks that re-map imports and require calls to support:</p>
<ul>
<li>remapping extensions, e.g. so that <code v-pre>import &quot;./foo.js&quot;</code> will execute <code v-pre>foo.ts</code>. Currently the following extensions will be mapped:
<ul>
<li><code v-pre>.js</code> to <code v-pre>.ts</code>, <code v-pre>.tsx</code>, or <code v-pre>.jsx</code></li>
<li><code v-pre>.cjs</code> to <code v-pre>.cts</code></li>
<li><code v-pre>.mjs</code> to <code v-pre>.mts</code></li>
<li><code v-pre>.jsx</code> to <code v-pre>.tsx</code></li>
</ul>
</li>
<li>including file extensions in CommonJS, for consistency with ESM where this is often mandatory</li>
</ul>
<p>In the future, this hook will also support:</p>
<ul>
<li><code v-pre>baseUrl</code>, <code v-pre>paths</code></li>
<li><code v-pre>rootDirs</code></li>
<li><code v-pre>outDir</code> to <code v-pre>rootDir</code> mappings for composite projects and monorepos</li>
</ul>
<p>For details, see <a href="https://github.com/TypeStrong/ts-node/issues/1514" target="_blank" rel="noopener noreferrer">#1514<ExternalLinkIcon/></a>.</p>
<p><em>Default:</em> <code v-pre>false</code>, but will likely be enabled by default in a future version<br/>
<em>Can only be specified via <code v-pre>tsconfig.json</code> or API.</em></p>
<h3 id="experimentalspecifierresolution" tabindex="-1"><a class="header-anchor" href="#experimentalspecifierresolution" aria-hidden="true">#</a> experimentalSpecifierResolution</h3>
<div class="language-bash ext-sh line-numbers-mode"><pre v-pre class="language-bash"><code>ts-node --experimentalSpecifierResolution <span class="token function">node</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Like node's <a href="https://nodejs.org/dist/latest-v18.x/docs/api/esm.html#customizing-esm-specifier-resolution-algorithm" target="_blank" rel="noopener noreferrer"><code v-pre>--experimental-specifier-resolution</code><ExternalLinkIcon/></a>, but can also be set in your <code v-pre>tsconfig.json</code> for convenience.
Requires <a href="#esm"><code v-pre>esm</code></a> to be enabled.</p>
<p><em>Default:</em> <code v-pre>explicit</code><br/></p>
<h2 id="api-options" tabindex="-1"><a class="header-anchor" href="#api-options" aria-hidden="true">#</a> API Options</h2>
<p>The API includes <a href="https://typestrong.org/ts-node/api/interfaces/RegisterOptions.html" target="_blank" rel="noopener noreferrer">additional options<ExternalLinkIcon/></a> not shown here.</p>
<h1 id="swc-1" tabindex="-1"><a class="header-anchor" href="#swc-1" aria-hidden="true">#</a> SWC</h1>
<p>SWC support is built-in via the <code v-pre>--swc</code> flag or <code v-pre>&quot;swc&quot;: true</code> tsconfig option.</p>
<p><a href="https://swc.rs" target="_blank" rel="noopener noreferrer">SWC<ExternalLinkIcon/></a> is a TypeScript-compatible transpiler implemented in Rust.  This makes it an order of magnitude faster than vanilla <code v-pre>transpileOnly</code>.</p>
<p>To use it, first install <code v-pre>@swc/core</code> or <code v-pre>@swc/wasm</code>.  If using <code v-pre>importHelpers</code>, also install <code v-pre>@swc/helpers</code>.  If <code v-pre>target</code> is less than &quot;es2015&quot; and using <code v-pre>async</code>/<code v-pre>await</code> or generator functions, also install <code v-pre>regenerator-runtime</code>.</p>
<div class="language-bash ext-sh line-numbers-mode"><pre v-pre class="language-bash"><code><span class="token function">npm</span> i -D @swc/core @swc/helpers regenerator-runtime
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Then add the following to your <code v-pre>tsconfig.json</code>.</p>
<div class="language-jsonc ext-jsonc line-numbers-mode"><pre v-pre class="language-jsonc"><code>{
  &quot;ts-node&quot;: {
    &quot;swc&quot;: true
  }
}
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><blockquote>
<p>SWC uses <code v-pre>@swc/helpers</code> instead of <code v-pre>tslib</code>.  If you have enabled <code v-pre>importHelpers</code>, you must also install <code v-pre>@swc/helpers</code>.</p>
</blockquote>
<h1 id="commonjs-vs-native-ecmascript-modules" tabindex="-1"><a class="header-anchor" href="#commonjs-vs-native-ecmascript-modules" aria-hidden="true">#</a> CommonJS vs native ECMAScript modules</h1>
<p>TypeScript is almost always written using modern <code v-pre>import</code> syntax, but it is also transformed before being executed by the underlying runtime.  You can choose to either transform to CommonJS or to preserve the native <code v-pre>import</code> syntax, using node's native ESM support.  Configuration is different for each.</p>
<p>Here is a brief comparison of the two.</p>
<table>
<thead>
<tr>
<th>CommonJS</th>
<th>Native ECMAScript modules</th>
</tr>
</thead>
<tbody>
<tr>
<td>Write native <code v-pre>import</code> syntax</td>
<td>Write native <code v-pre>import</code> syntax</td>
</tr>
<tr>
<td>Transforms <code v-pre>import</code> into <code v-pre>require()</code></td>
<td>Does not transform <code v-pre>import</code></td>
</tr>
<tr>
<td>Node executes scripts using the classic <a href="https://nodejs.org/dist/latest-v16.x/docs/api/modules.html" target="_blank" rel="noopener noreferrer">CommonJS loader<ExternalLinkIcon/></a></td>
<td>Node executes scripts using the new <a href="https://nodejs.org/dist/latest-v16.x/docs/api/esm.html" target="_blank" rel="noopener noreferrer">ESM loader<ExternalLinkIcon/></a></td>
</tr>
<tr>
<td>Use any of:<br/><code v-pre>ts-node</code><br/><code v-pre>node -r ts-node/register</code><br/><code v-pre>NODE_OPTIONS=&quot;ts-node/register&quot; node</code><br/><code v-pre>require('ts-node').register({/* options */})</code></td>
<td>Use any of:<br/><code v-pre>ts-node --esm</code><br/><code v-pre>ts-node-esm</code><br/>Set <code v-pre>&quot;esm&quot;: true</code> in <code v-pre>tsconfig.json</code><br /><code v-pre>node --loader ts-node/esm</code><br/><code v-pre>NODE_OPTIONS=&quot;--loader ts-node/esm&quot; node</code></td>
</tr>
</tbody>
</table>
<h2 id="commonjs" tabindex="-1"><a class="header-anchor" href="#commonjs" aria-hidden="true">#</a> CommonJS</h2>
<p>Transforming to CommonJS is typically simpler and more widely supported because it is older.  You must remove <a href="https://nodejs.org/api/packages.html#packages_type" target="_blank" rel="noopener noreferrer"><code v-pre>&quot;type&quot;: &quot;module&quot;</code><ExternalLinkIcon/></a> from <code v-pre>package.json</code> and set <a href="https://www.typescriptlang.org/tsconfig/#module" target="_blank" rel="noopener noreferrer"><code v-pre>&quot;module&quot;: &quot;CommonJS&quot;</code><ExternalLinkIcon/></a> in <code v-pre>tsconfig.json</code>.</p>
<div class="language-jsonc ext-jsonc line-numbers-mode"><pre v-pre class="language-jsonc"><code>{
  // This can be omitted; commonjs is the default
  &quot;type&quot;: &quot;commonjs&quot;
}
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><div class="language-jsonc ext-jsonc line-numbers-mode"><pre v-pre class="language-jsonc"><code>{
  &quot;compilerOptions&quot;: {
    &quot;module&quot;: &quot;CommonJS&quot;
  }
}
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>If you must keep <code v-pre>&quot;module&quot;: &quot;ESNext&quot;</code> for <code v-pre>tsc</code>, webpack, or another build tool, you can set an override for ts-node.</p>
<div class="language-jsonc ext-jsonc line-numbers-mode"><pre v-pre class="language-jsonc"><code>{
  &quot;compilerOptions&quot;: {
    &quot;module&quot;: &quot;ESNext&quot;
  },
  &quot;ts-node&quot;: {
    &quot;compilerOptions&quot;: {
      &quot;module&quot;: &quot;CommonJS&quot;
    }
  }
}
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="native-ecmascript-modules" tabindex="-1"><a class="header-anchor" href="#native-ecmascript-modules" aria-hidden="true">#</a> Native ECMAScript modules</h2>
<p><a href="https://nodejs.org/api/esm.html#esm_experimental_loaders" target="_blank" rel="noopener noreferrer">Node's ESM loader hooks<ExternalLinkIcon/></a> are <a href="https://nodejs.org/api/documentation.html#documentation_stability_index" target="_blank" rel="noopener noreferrer"><strong>experimental</strong><ExternalLinkIcon/></a> and subject to change. ts-node's ESM support is as stable as possible, but it relies on APIs which node can <em>and will</em> break in new versions of node.  Thus it is not recommended for production.</p>
<p>For complete usage, limitations, and to provide feedback, see <a href="https://github.com/TypeStrong/ts-node/issues/1007" target="_blank" rel="noopener noreferrer">#1007<ExternalLinkIcon/></a>.</p>
<p>You must set <a href="https://nodejs.org/api/packages.html#packages_type" target="_blank" rel="noopener noreferrer"><code v-pre>&quot;type&quot;: &quot;module&quot;</code><ExternalLinkIcon/></a> in <code v-pre>package.json</code> and <a href="https://www.typescriptlang.org/tsconfig/#module" target="_blank" rel="noopener noreferrer"><code v-pre>&quot;module&quot;: &quot;ESNext&quot;</code><ExternalLinkIcon/></a> in <code v-pre>tsconfig.json</code>.</p>
<div class="language-jsonc ext-jsonc line-numbers-mode"><pre v-pre class="language-jsonc"><code>{
  &quot;type&quot;: &quot;module&quot;
}
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><div class="language-jsonc ext-jsonc line-numbers-mode"><pre v-pre class="language-jsonc"><code>{
  &quot;compilerOptions&quot;: {
    &quot;module&quot;: &quot;ESNext&quot; // or ES2015, ES2020
  },
  &quot;ts-node&quot;: {
    // Tell ts-node CLI to install the --loader automatically, explained below
    &quot;esm&quot;: true
  }
}
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>You must also ensure node is passed <code v-pre>--loader</code>.  The ts-node CLI will do this automatically with our <code v-pre>esm</code> option.</p>
<blockquote>
<p>Note: <code v-pre>--esm</code> must spawn a child process to pass it <code v-pre>--loader</code>.  This may change if node adds the ability to install loader hooks
into the current process.</p>
</blockquote>
<div class="language-bash ext-sh line-numbers-mode"><pre v-pre class="language-bash"><code><span class="token comment"># pass the flag</span>
ts-node --esm
<span class="token comment"># Use the convenience binary</span>
ts-node-esm
<span class="token comment"># or add `"esm": true` to your tsconfig.json to make it automatic</span>
ts-node
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>If you are not using our CLI, pass the loader flag to node.</p>
<div class="language-bash ext-sh line-numbers-mode"><pre v-pre class="language-bash"><code><span class="token function">node</span> --loader ts-node/esm ./index.ts
<span class="token comment"># Or via environment variable</span>
<span class="token assign-left variable">NODE_OPTIONS</span><span class="token operator">=</span><span class="token string">"--loader ts-node/esm"</span> <span class="token function">node</span> ./index.ts
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h1 id="troubleshooting" tabindex="-1"><a class="header-anchor" href="#troubleshooting" aria-hidden="true">#</a> Troubleshooting</h1>
<h2 id="configuration-1" tabindex="-1"><a class="header-anchor" href="#configuration-1" aria-hidden="true">#</a> Configuration</h2>
<p>ts-node uses sensible default configurations to reduce boilerplate while still respecting <code v-pre>tsconfig.json</code> if you
have one.  If you are unsure which configuration is used, you can log it with <code v-pre>ts-node --showConfig</code>.  This is similar to
<code v-pre>tsc --showConfig</code> but includes <code v-pre>&quot;ts-node&quot;</code> options as well.</p>
<p>ts-node also respects your locally-installed <code v-pre>typescript</code> version, but global installations fallback to the globally-installed
<code v-pre>typescript</code>.  If you are unsure which versions are used, <code v-pre>ts-node -vv</code> will log them.</p>
<div class="language-bash ext-sh line-numbers-mode"><pre v-pre class="language-bash"><code>$ ts-node -vv
ts-node v10.0.0
<span class="token function">node</span> v16.1.0
compiler v4.2.2

$ ts-node --showConfig
<span class="token punctuation">{</span>
  <span class="token string">"compilerOptions"</span><span class="token builtin class-name">:</span> <span class="token punctuation">{</span>
    <span class="token string">"target"</span><span class="token builtin class-name">:</span> <span class="token string">"es6"</span>,
    <span class="token string">"lib"</span><span class="token builtin class-name">:</span> <span class="token punctuation">[</span>
      <span class="token string">"es6"</span>,
      <span class="token string">"dom"</span>
    <span class="token punctuation">]</span>,
    <span class="token string">"rootDir"</span><span class="token builtin class-name">:</span> <span class="token string">"./src"</span>,
    <span class="token string">"outDir"</span><span class="token builtin class-name">:</span> <span class="token string">"./.ts-node"</span>,
    <span class="token string">"module"</span><span class="token builtin class-name">:</span> <span class="token string">"commonjs"</span>,
    <span class="token string">"moduleResolution"</span><span class="token builtin class-name">:</span> <span class="token string">"node"</span>,
    <span class="token string">"strict"</span><span class="token builtin class-name">:</span> true,
    <span class="token string">"declaration"</span><span class="token builtin class-name">:</span> false,
    <span class="token string">"sourceMap"</span><span class="token builtin class-name">:</span> true,
    <span class="token string">"inlineSources"</span><span class="token builtin class-name">:</span> true,
    <span class="token string">"types"</span><span class="token builtin class-name">:</span> <span class="token punctuation">[</span>
      <span class="token string">"node"</span>
    <span class="token punctuation">]</span>,
    <span class="token string">"stripInternal"</span><span class="token builtin class-name">:</span> true,
    <span class="token string">"incremental"</span><span class="token builtin class-name">:</span> true,
    <span class="token string">"skipLibCheck"</span><span class="token builtin class-name">:</span> true,
    <span class="token string">"importsNotUsedAsValues"</span><span class="token builtin class-name">:</span> <span class="token string">"error"</span>,
    <span class="token string">"inlineSourceMap"</span><span class="token builtin class-name">:</span> false,
    <span class="token string">"noEmit"</span><span class="token builtin class-name">:</span> <span class="token boolean">false</span>
  <span class="token punctuation">}</span>,
  <span class="token string">"ts-node"</span><span class="token builtin class-name">:</span> <span class="token punctuation">{</span>
    <span class="token string">"cwd"</span><span class="token builtin class-name">:</span> <span class="token string">"/d/project"</span>,
    <span class="token string">"projectSearchDir"</span><span class="token builtin class-name">:</span> <span class="token string">"/d/project"</span>,
    <span class="token string">"require"</span><span class="token builtin class-name">:</span> <span class="token punctuation">[</span><span class="token punctuation">]</span>,
    <span class="token string">"project"</span><span class="token builtin class-name">:</span> <span class="token string">"/d/project/tsconfig.json"</span>
  <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="common-errors" tabindex="-1"><a class="header-anchor" href="#common-errors" aria-hidden="true">#</a> Common errors</h2>
<p>It is important to differentiate between errors from ts-node, errors from the TypeScript compiler, and errors from <code v-pre>node</code>.  It is also important to understand when errors are caused by a type error in your code, a bug in your code, or a flaw in your configuration.</p>
<h3 id="tserror" tabindex="-1"><a class="header-anchor" href="#tserror" aria-hidden="true">#</a> <code v-pre>TSError</code></h3>
<p>Type errors from the compiler are thrown as a <code v-pre>TSError</code>.  These are the same as errors you get from <code v-pre>tsc</code>.</p>
<h3 id="syntaxerror" tabindex="-1"><a class="header-anchor" href="#syntaxerror" aria-hidden="true">#</a> <code v-pre>SyntaxError</code></h3>
<p>Any error that is not a <code v-pre>TSError</code> is from node.js (e.g. <code v-pre>SyntaxError</code>), and cannot be fixed by TypeScript or ts-node. These are bugs in your code or configuration.</p>
<h4 id="unsupported-javascript-syntax" tabindex="-1"><a class="header-anchor" href="#unsupported-javascript-syntax" aria-hidden="true">#</a> Unsupported JavaScript syntax</h4>
<p>Your version of <code v-pre>node</code> may not support all JavaScript syntax supported by TypeScript.  The compiler must transform this syntax via &quot;downleveling,&quot; which is controlled by
the <a href="https://www.typescriptlang.org/tsconfig#target" target="_blank" rel="noopener noreferrer">tsconfig <code v-pre>&quot;target&quot;</code> option<ExternalLinkIcon/></a>.  Otherwise your code will compile fine, but node will throw a <code v-pre>SyntaxError</code>.</p>
<p>For example, <code v-pre>node</code> 12 does not understand the <code v-pre>?.</code> optional chaining operator.  If you use <code v-pre>&quot;target&quot;: &quot;esnext&quot;</code>, then the following TypeScript syntax:</p>
<div class="language-typescript ext-ts line-numbers-mode"><pre v-pre class="language-typescript"><code><span class="token keyword">const</span> bar<span class="token operator">:</span> <span class="token builtin">string</span> <span class="token operator">|</span> <span class="token keyword">undefined</span> <span class="token operator">=</span> foo<span class="token operator">?.</span>bar<span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>will compile into this JavaScript:</p>
<div class="language-javascript ext-js line-numbers-mode"><pre v-pre class="language-javascript"><code><span class="token keyword">const</span> a <span class="token operator">=</span> foo<span class="token operator">?.</span>bar<span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>When you try to run this code, node 12 will throw a <code v-pre>SyntaxError</code>.  To fix this, you must switch to <code v-pre>&quot;target&quot;: &quot;es2019&quot;</code> or lower so TypeScript transforms <code v-pre>?.</code> into something <code v-pre>node</code> can understand.</p>
<h3 id="err-require-esm" tabindex="-1"><a class="header-anchor" href="#err-require-esm" aria-hidden="true">#</a> <code v-pre>ERR_REQUIRE_ESM</code></h3>
<p>This error is thrown by node when a module is <code v-pre>require()</code>d, but node believes it should execute as native ESM.  This can happen for a few reasons:</p>
<ul>
<li>You have installed an ESM dependency but your own code compiles to CommonJS.
<ul>
<li>Solution: configure your project to compile and execute as native ESM. <a href="#native-ecmascript-modules">Docs</a></li>
<li>Solution: downgrade the dependency to an older, CommonJS version.</li>
</ul>
</li>
<li>You have moved your project to ESM but still have a config file, such as <code v-pre>webpack.config.ts</code>, which must be executed as CommonJS <!-- SYNC_WITH_MTO_DOCS -->
<ul>
<li>Solution: if supported by the relevant tool, rename your config file to <code v-pre>.cts</code></li>
<li>Solution: Configure a module type override. <a href="#module-type-overrides">Docs</a></li>
</ul>
</li>
<li>You have a mix of CommonJS and native ESM in your project
<ul>
<li>Solution: double-check all package.json &quot;type&quot; and tsconfig.json &quot;module&quot; configuration <a href="#commonjs-vs-native-ecmascript-modules">Docs</a></li>
<li>Solution: consider simplifying by making your project entirely CommonJS or entirely native ESM</li>
</ul>
</li>
</ul>
<h3 id="err-unknown-file-extension" tabindex="-1"><a class="header-anchor" href="#err-unknown-file-extension" aria-hidden="true">#</a> <code v-pre>ERR_UNKNOWN_FILE_EXTENSION</code></h3>
<p>This error is thrown by node when a module has an unrecognized file extension, or no extension at all, and is being executed as native ESM.  This can happen for a few reasons:</p>
<ul>
<li>You are using a tool which has an extensionless binary, such as <code v-pre>mocha</code>.
<ul>
<li>CommonJS supports extensionless files but native ESM does not.</li>
<li>Solution: upgrade to ts-node &gt;=<a href="https://github.com/TypeStrong/ts-node/releases/tag/v10.6.0" target="_blank" rel="noopener noreferrer">v10.6.0<ExternalLinkIcon/></a>, which implements a workaround.</li>
</ul>
</li>
<li>Our ESM loader is not installed.
<ul>
<li>Solution: Use <code v-pre>ts-node-esm</code>, <code v-pre>ts-node --esm</code>, or add <code v-pre>&quot;ts-node&quot;: {&quot;esm&quot;: true}</code> to your tsconfig.json.  <a href="#native-ecmascript-modules">Docs</a></li>
</ul>
</li>
<li>You have moved your project to ESM but still have a config file, such as <code v-pre>webpack.config.ts</code>, which must be executed as CommonJS <!-- SYNC_WITH_MTO_DOCS -->
<ul>
<li>Solution: if supported by the relevant tool, rename your config file to <code v-pre>.cts</code></li>
<li>Solution: Configure a module type override. <a href="#module-type-overrides">Docs</a></li>
</ul>
</li>
</ul>
<h2 id="missing-types" tabindex="-1"><a class="header-anchor" href="#missing-types" aria-hidden="true">#</a> Missing Types</h2>
<p>ts-node does <em>not</em> eagerly load <code v-pre>files</code>, <code v-pre>include</code> or <code v-pre>exclude</code> by default. This is because a large majority of projects do not use all of the files in a project directory (e.g. <code v-pre>Gulpfile.ts</code>, runtime vs tests) and parsing every file for types slows startup time. Instead, ts-node starts with the script file (e.g. <code v-pre>ts-node index.ts</code>) and TypeScript resolves dependencies based on imports and references.</p>
<p>Occasionally, this optimization leads to missing types. Fortunately, there are other ways to include them in typechecking.</p>
<p>For global definitions, you can use the <code v-pre>typeRoots</code> compiler option.  This requires that your type definitions be structured as type packages (not loose TypeScript definition files). More details on how this works can be found in the <a href="https://www.typescriptlang.org/docs/handbook/tsconfig-json.html#types-typeroots-and-types" target="_blank" rel="noopener noreferrer">TypeScript Handbook<ExternalLinkIcon/></a>.</p>
<p>Example <code v-pre>tsconfig.json</code>:</p>
<div class="language-jsonc ext-jsonc line-numbers-mode"><pre v-pre class="language-jsonc"><code>{
  &quot;compilerOptions&quot;: {
    &quot;typeRoots&quot; : [&quot;./node_modules/@types&quot;, &quot;./typings&quot;]
  }
}
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Example project structure:</p>
<div class="language-text ext-text line-numbers-mode"><pre v-pre class="language-text"><code>&lt;project_root>/
-- tsconfig.json
-- typings/
  -- &lt;module_name>/
    -- index.d.ts
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Example module declaration file:</p>
<div class="language-typescript ext-ts line-numbers-mode"><pre v-pre class="language-typescript"><code><span class="token keyword">declare</span> <span class="token keyword">module</span> <span class="token string">'&lt;module_name>'</span> <span class="token punctuation">{</span>
    <span class="token comment">// module definitions go here</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>For module definitions, you can use <a href="https://www.typescriptlang.org/docs/handbook/module-resolution.html#path-mapping" target="_blank" rel="noopener noreferrer"><code v-pre>paths</code><ExternalLinkIcon/></a>:</p>
<div class="language-jsonc ext-jsonc line-numbers-mode"><pre v-pre class="language-jsonc"><code>{
  &quot;compilerOptions&quot;: {
    &quot;baseUrl&quot;: &quot;.&quot;,
    &quot;paths&quot;: {
      &quot;custom-module-type&quot;: [&quot;types/custom-module-type&quot;]
    }
  }
}
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Another option is <a href="https://www.typescriptlang.org/docs/handbook/triple-slash-directives.html" target="_blank" rel="noopener noreferrer">triple-slash directives<ExternalLinkIcon/></a>. This may be helpful if you prefer not to change your <code v-pre>compilerOptions</code> or structure your type definitions for <code v-pre>typeRoots</code>. Below is an example of a triple-slash directive as a relative path within your project:</p>
<div class="language-typescript ext-ts line-numbers-mode"><pre v-pre class="language-typescript"><code><span class="token comment">/// &lt;reference path="./types/lib_greeter" /></span>
<span class="token keyword">import</span> <span class="token punctuation">{</span>Greeter<span class="token punctuation">}</span> <span class="token keyword">from</span> <span class="token string">"lib_greeter"</span>
<span class="token keyword">const</span> g <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">Greeter</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
g<span class="token punctuation">.</span><span class="token function">sayHello</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>If none of the above work, and you <em>must</em> use <code v-pre>files</code>, <code v-pre>include</code>, or <code v-pre>exclude</code>, enable our <a href="#files"><code v-pre>files</code></a> option.</p>
<h2 id="npx-yarn-dlx-and-node-modules" tabindex="-1"><a class="header-anchor" href="#npx-yarn-dlx-and-node-modules" aria-hidden="true">#</a> npx, yarn dlx, and node_modules</h2>
<p>When executing TypeScript with <code v-pre>npx</code> or <code v-pre>yarn dlx</code>, the code resides within a temporary <code v-pre>node_modules</code> directory.</p>
<p>The contents of <code v-pre>node_modules</code> are ignored by default.  If execution fails, enable <a href="#skipignore"><code v-pre>skipIgnore</code></a>.</p>
<!--See also: [npx and yarn dlx](./recipes/npx-and-yarn-dlx.md)-->
<h1 id="performance" tabindex="-1"><a class="header-anchor" href="#performance" aria-hidden="true">#</a> Performance</h1>
<p>These tricks will make ts-node faster.</p>
<h2 id="skip-typechecking" tabindex="-1"><a class="header-anchor" href="#skip-typechecking" aria-hidden="true">#</a> Skip typechecking</h2>
<p>It is often better to typecheck as part of your tests or linting.  You can run <code v-pre>tsc --noEmit</code> to do this.  In these cases, ts-node can skip typechecking, making it much faster.</p>
<p>To skip typechecking in ts-node, do one of the following:</p>
<ul>
<li>Enable <a href="#swc">swc</a>
<ul>
<li>This is by far the fastest option</li>
</ul>
</li>
<li>Enable <a href="#transpileonly"><code v-pre>transpileOnly</code></a> to skip typechecking without swc</li>
</ul>
<h2 id="with-typechecking" tabindex="-1"><a class="header-anchor" href="#with-typechecking" aria-hidden="true">#</a> With typechecking</h2>
<p>If you absolutely must typecheck in ts-node:</p>
<ul>
<li>Avoid dynamic <code v-pre>require()</code> which may trigger repeated typechecking; prefer <code v-pre>import</code></li>
<li>Try with and without <code v-pre>--files</code>; one may be faster depending on your project</li>
<li>Check <code v-pre>tsc --showConfig</code>; make sure all executed files are included</li>
<li>Enable <a href="https://www.typescriptlang.org/tsconfig#skipLibCheck" target="_blank" rel="noopener noreferrer"><code v-pre>skipLibCheck</code><ExternalLinkIcon/></a></li>
<li>Set a <a href="https://www.typescriptlang.org/tsconfig#types" target="_blank" rel="noopener noreferrer"><code v-pre>types</code><ExternalLinkIcon/></a> array to avoid loading unnecessary <code v-pre>@types</code></li>
</ul>
<h1 id="advanced" tabindex="-1"><a class="header-anchor" href="#advanced" aria-hidden="true">#</a> Advanced</h1>
<h2 id="how-it-works" tabindex="-1"><a class="header-anchor" href="#how-it-works" aria-hidden="true">#</a> How it works</h2>
<p>ts-node works by registering hooks for <code v-pre>.ts</code>, <code v-pre>.tsx</code>, <code v-pre>.js</code>, and/or <code v-pre>.jsx</code> extensions.</p>
<p>Vanilla <code v-pre>node</code> loads <code v-pre>.js</code> by reading code from disk and executing it.  Our hook runs in the middle, transforming code from TypeScript to JavaScript and passing the result to <code v-pre>node</code> for execution.  This transformation will respect your <code v-pre>tsconfig.json</code> as if you had compiled via <code v-pre>tsc</code>.</p>
<p>We also register a few other hooks to apply sourcemaps to stack traces and remap from <code v-pre>.js</code> imports to <code v-pre>.ts</code>.</p>
<h2 id="ignored-files" tabindex="-1"><a class="header-anchor" href="#ignored-files" aria-hidden="true">#</a> Ignored files</h2>
<p>ts-node transforms certain files and ignores others.  We refer to this mechanism as &quot;scoping.&quot;  There are various
options to configure scoping, so that ts-node transforms only the files in your project.</p>
<blockquote>
<p><strong>Warning:</strong></p>
<p>An ignored file can still be executed by node.js.  Ignoring a file means we do not transform it from TypeScript into JavaScript, but it does not prevent execution.</p>
<p>If a file requires transformation but is ignored, node may either fail to resolve it or attempt to execute it as vanilla JavaScript.  This may cause syntax errors or other failures, because node does not understand TypeScript type syntax nor bleeding-edge ECMAScript features.</p>
</blockquote>
<h3 id="file-extensions" tabindex="-1"><a class="header-anchor" href="#file-extensions" aria-hidden="true">#</a> File extensions</h3>
<p><code v-pre>.js</code> and <code v-pre>.jsx</code> are only transformed when <a href="https://www.typescriptlang.org/docs/handbook/compiler-options.html#compiler-options" target="_blank" rel="noopener noreferrer"><code v-pre>allowJs</code><ExternalLinkIcon/></a> is enabled.</p>
<p><code v-pre>.tsx</code> and <code v-pre>.jsx</code> are only transformed when <a href="https://www.typescriptlang.org/docs/handbook/jsx.html" target="_blank" rel="noopener noreferrer"><code v-pre>jsx</code><ExternalLinkIcon/></a> is enabled.</p>
<blockquote>
<p><strong>Warning:</strong></p>
<p>When ts-node is used with <code v-pre>allowJs</code>, <em>all</em> non-ignored JavaScript files are transformed by ts-node.</p>
</blockquote>
<h3 id="skipping-node-modules" tabindex="-1"><a class="header-anchor" href="#skipping-node-modules" aria-hidden="true">#</a> Skipping <code v-pre>node_modules</code></h3>
<p>By default, ts-node avoids compiling files in <code v-pre>/node_modules/</code> for three reasons:</p>
<ol>
<li>Modules should always be published in a format node.js can consume</li>
<li>Transpiling the entire dependency tree will make your project slower</li>
<li>Differing behaviours between TypeScript and node.js (e.g. ES2015 modules) can result in a project that works until you decide to support a feature natively from node.js</li>
</ol>
<p>If you need to import uncompiled TypeScript in <code v-pre>node_modules</code>, use <a href="#skipignore"><code v-pre>--skipIgnore</code></a> or <a href="#skipignore"><code v-pre>TS_NODE_SKIP_IGNORE</code></a> to bypass this restriction.</p>
<h3 id="skipping-pre-compiled-typescript" tabindex="-1"><a class="header-anchor" href="#skipping-pre-compiled-typescript" aria-hidden="true">#</a> Skipping pre-compiled TypeScript</h3>
<p>If a compiled JavaScript file with the same name as a TypeScript file already exists, the TypeScript file will be ignored.  ts-node will import the pre-compiled JavaScript.</p>
<p>To force ts-node to import the TypeScript source, not the precompiled JavaScript, use <a href="#prefertsexts"><code v-pre>--preferTsExts</code></a>.</p>
<h3 id="scope-by-directory" tabindex="-1"><a class="header-anchor" href="#scope-by-directory" aria-hidden="true">#</a> Scope by directory</h3>
<p>Our <a href="#scope"><code v-pre>scope</code></a> and <a href="#scopedir"><code v-pre>scopeDir</code></a> options will limit transformation to files
within a directory.</p>
<h3 id="ignore-by-regexp" tabindex="-1"><a class="header-anchor" href="#ignore-by-regexp" aria-hidden="true">#</a> Ignore by regexp</h3>
<p>Our <a href="#ignore"><code v-pre>ignore</code></a> option will ignore files matching one or more regular expressions.</p>
<h2 id="paths-and-baseurl" tabindex="-1"><a class="header-anchor" href="#paths-and-baseurl" aria-hidden="true">#</a> paths and baseUrl
</h2>
<p>You can use ts-node together with <a href="https://www.npmjs.com/package/tsconfig-paths" target="_blank" rel="noopener noreferrer">tsconfig-paths<ExternalLinkIcon/></a> to load modules according to the <code v-pre>paths</code> section in <code v-pre>tsconfig.json</code>.</p>
<div class="language-jsonc ext-jsonc line-numbers-mode"><pre v-pre class="language-jsonc"><code>{
  &quot;ts-node&quot;: {
    // Do not forget to `npm i -D tsconfig-paths`
    &quot;require&quot;: [&quot;tsconfig-paths/register&quot;]
  }
}
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h3 id="why-is-this-not-built-in-to-ts-node" tabindex="-1"><a class="header-anchor" href="#why-is-this-not-built-in-to-ts-node" aria-hidden="true">#</a> Why is this not built-in to ts-node?</h3>
<p>The official TypeScript Handbook explains the intended purpose for <code v-pre>&quot;paths&quot;</code> in <a href="https://www.typescriptlang.org/docs/handbook/module-resolution.html#additional-module-resolution-flags" target="_blank" rel="noopener noreferrer">&quot;Additional module resolution flags&quot;<ExternalLinkIcon/></a>.</p>
<blockquote>
<p>The TypeScript compiler has a set of additional flags to <em>inform</em> the compiler of transformations that are expected to happen to the sources to generate the final output.</p>
<p>It is important to note that the compiler will not perform any of these transformations; it just uses these pieces of information to guide the process of resolving a module import to its definition file.</p>
</blockquote>
<p>This means <code v-pre>&quot;paths&quot;</code> are intended to describe mappings that the build tool or runtime <em>already</em> performs, not to tell the build tool or
runtime how to resolve modules.  In other words, they intend us to write our imports in a way <code v-pre>node</code> already understands.  For this reason, ts-node does not modify <code v-pre>node</code>'s module resolution behavior to implement <code v-pre>&quot;paths&quot;</code> mappings.</p>
<h2 id="third-party-compilers" tabindex="-1"><a class="header-anchor" href="#third-party-compilers" aria-hidden="true">#</a> Third-party compilers</h2>
<p>Some projects require a patched typescript compiler which adds additional features.  For example, <a href="https://github.com/cevek/ttypescript/tree/master/packages/ttypescript" target="_blank" rel="noopener noreferrer"><code v-pre>ttypescript</code><ExternalLinkIcon/></a> and <a href="https://github.com/nonara/ts-patch#readme" target="_blank" rel="noopener noreferrer"><code v-pre>ts-patch</code><ExternalLinkIcon/></a>
add the ability to configure custom transformers.  These are drop-in replacements for the vanilla <code v-pre>typescript</code> module and
implement the same API.</p>
<p>For example, to use <code v-pre>ttypescript</code> and <code v-pre>ts-transformer-keys</code>, add this to your <code v-pre>tsconfig.json</code>:</p>
<div class="language-jsonc ext-jsonc line-numbers-mode"><pre v-pre class="language-jsonc"><code>{
  &quot;ts-node&quot;: {
    // This can be omitted when using ts-patch
    &quot;compiler&quot;: &quot;ttypescript&quot;
  },
  &quot;compilerOptions&quot;: {
    // plugin configuration is the same for both ts-patch and ttypescript
    &quot;plugins&quot;: [
      { &quot;transform&quot;: &quot;ts-transformer-keys/transformer&quot; }
    ]
  }
}
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="transpilers" tabindex="-1"><a class="header-anchor" href="#transpilers" aria-hidden="true">#</a> Transpilers</h2>
<p>ts-node supports third-party transpilers as plugins.  Transpilers such as swc can transform TypeScript into JavaScript
much faster than the TypeScript compiler.  You will still benefit from ts-node's automatic <code v-pre>tsconfig.json</code> discovery,
sourcemap support, and global ts-node CLI. Plugins automatically derive an appropriate configuration from your existing
<code v-pre>tsconfig.json</code> which simplifies project boilerplate.</p>
<blockquote>
<p><strong>What is the difference between a compiler and a transpiler?</strong></p>
<p>For our purposes, a compiler implements TypeScript's API and can perform typechecking.
A third-party transpiler does not.  Both transform TypeScript into JavaScript.</p>
</blockquote>
<h3 id="third-party-plugins" tabindex="-1"><a class="header-anchor" href="#third-party-plugins" aria-hidden="true">#</a> Third-party plugins</h3>
<p>The <code v-pre>transpiler</code> option allows using third-party transpiler plugins with ts-node.  <code v-pre>transpiler</code> must be given the
name of a module which can be <code v-pre>require()</code>d.  The built-in <code v-pre>swc</code> plugin is exposed as <code v-pre>ts-node/transpilers/swc</code>.</p>
<p>For example, to use a hypothetical &quot;@cspotcode/fast-ts-compiler&quot;, first install it into your project: <code v-pre>npm install @cspotcode/fast-ts-compiler</code></p>
<p>Then add the following to your tsconfig:</p>
<div class="language-jsonc ext-jsonc line-numbers-mode"><pre v-pre class="language-jsonc"><code>{
  &quot;ts-node&quot;: {
    &quot;transpileOnly&quot;: true,
    &quot;transpiler&quot;: &quot;@cspotcode/fast-ts-compiler&quot;
  }
}
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h3 id="write-your-own-plugin" tabindex="-1"><a class="header-anchor" href="#write-your-own-plugin" aria-hidden="true">#</a> Write your own plugin</h3>
<p>To write your own transpiler plugin, check our <a href="https://typestrong.org/ts-node/api/interfaces/TranspilerModule.html" target="_blank" rel="noopener noreferrer">API docs<ExternalLinkIcon/></a>.</p>
<p>Plugins are <code v-pre>require()</code>d by ts-node, so they can be a local script or a node module published to npm.  The module must
export a <code v-pre>create</code> function described by our
<a href="https://typestrong.org/ts-node/api/interfaces/TranspilerModule.html" target="_blank" rel="noopener noreferrer"><code v-pre>TranspilerModule</code><ExternalLinkIcon/></a> interface.  <code v-pre>create</code> is
invoked by ts-node at startup to create one or more transpiler instances.  The instances are used to transform
TypeScript into JavaScript.</p>
<p>For a working example, check out out our bundled swc plugin: https://github.com/TypeStrong/ts-node/blob/main/src/transpilers/swc.ts</p>
<h2 id="module-type-overrides" tabindex="-1"><a class="header-anchor" href="#module-type-overrides" aria-hidden="true">#</a> Module type overrides</h2>
<blockquote>
<p>Wherever possible, it is recommended to use TypeScript's <a href="https://www.typescriptlang.org/docs/handbook/esm-node.html" target="_blank" rel="noopener noreferrer"><code v-pre>NodeNext</code> or <code v-pre>Node16</code> mode<ExternalLinkIcon/></a> instead of the options described
in this section.  Setting <code v-pre>&quot;module&quot;: &quot;NodeNext&quot;</code> and using the <code v-pre>.cts</code> file extension should work well for most projects.</p>
</blockquote>
<p>When deciding how a file should be compiled and executed -- as either CommonJS or native ECMAScript module -- ts-node matches
<code v-pre>node</code> and <code v-pre>tsc</code> behavior.  This means TypeScript files are transformed according to your <code v-pre>tsconfig.json</code> <code v-pre>&quot;module&quot;</code>
option and executed according to node's rules for the <code v-pre>package.json</code> <code v-pre>&quot;type&quot;</code> field.  Set <code v-pre>&quot;module&quot;: &quot;NodeNext&quot;</code> and everything should work.</p>
<p>In rare cases, you may need to override this behavior for some files.  For example, some tools read a <code v-pre>name-of-tool.config.ts</code>
and require that file to execute as CommonJS.  If you have <code v-pre>package.json</code> configured with <code v-pre>&quot;type&quot;: &quot;module&quot;</code> and <code v-pre>tsconfig.json</code> with
<code v-pre>&quot;module&quot;: &quot;esnext&quot;</code>, the config is native ECMAScript by default and will raise an error.  You will need to force the config and
any supporting scripts to execute as CommonJS.</p>
<p>In these situations, our <code v-pre>moduleTypes</code> option can override certain files to be
CommonJS or ESM.  Similar overriding is possible by using <code v-pre>.mts</code>, <code v-pre>.cts</code>, <code v-pre>.cjs</code> and <code v-pre>.mjs</code> file extensions.
<code v-pre>moduleTypes</code> achieves the same effect for <code v-pre>.ts</code> and <code v-pre>.js</code> files, and <em>also</em> overrides your <code v-pre>tsconfig.json</code> <code v-pre>&quot;module&quot;</code>
config appropriately.</p>
<p>The following example tells ts-node to execute a webpack config as CommonJS:</p>
<div class="language-jsonc ext-jsonc line-numbers-mode"><pre v-pre class="language-jsonc"><code>{
  &quot;ts-node&quot;: {
    &quot;transpileOnly&quot;: true,
    &quot;moduleTypes&quot;: {
      &quot;webpack.config.ts&quot;: &quot;cjs&quot;,
      // Globs are also supported with the same behavior as tsconfig &quot;include&quot;
      &quot;webpack-config-scripts/**/*&quot;: &quot;cjs&quot;
    }
  },
  &quot;compilerOptions&quot;: {
    &quot;module&quot;: &quot;es2020&quot;,
    &quot;target&quot;: &quot;es2020&quot;
  }
}
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Each key is a glob pattern with the same syntax as tsconfig's <code v-pre>&quot;include&quot;</code> array.
When multiple patterns match the same file, the last pattern takes precedence.</p>
<ul>
<li><code v-pre>cjs</code> overrides matches files to compile and execute as CommonJS.</li>
<li><code v-pre>esm</code> overrides matches files to compile and execute as native ECMAScript modules.</li>
<li><code v-pre>package</code> resets either of the above to default behavior, which obeys <code v-pre>package.json</code> <code v-pre>&quot;type&quot;</code> and <code v-pre>tsconfig.json</code> <code v-pre>&quot;module&quot;</code> options.</li>
</ul>
<h3 id="caveats" tabindex="-1"><a class="header-anchor" href="#caveats" aria-hidden="true">#</a> Caveats</h3>
<p>Files with an overridden module type are transformed with the same limitations as <a href="https://www.typescriptlang.org/tsconfig#isolatedModules" target="_blank" rel="noopener noreferrer"><code v-pre>isolatedModules</code><ExternalLinkIcon/></a>.  This will only affect rare cases such as using <code v-pre>const enum</code>s with <a href="https://www.typescriptlang.org/tsconfig#preserveConstEnums" target="_blank" rel="noopener noreferrer"><code v-pre>preserveConstEnums</code><ExternalLinkIcon/></a> disabled.</p>
<p>This feature is meant to facilitate scenarios where normal <code v-pre>compilerOptions</code> and <code v-pre>package.json</code> configuration is not possible.  For example, a <code v-pre>webpack.config.ts</code> cannot be given its own <code v-pre>package.json</code> to override <code v-pre>&quot;type&quot;</code>.  Wherever possible you should favor using traditional <code v-pre>package.json</code> and <code v-pre>tsconfig.json</code> configurations.</p>
<h2 id="api" tabindex="-1"><a class="header-anchor" href="#api" aria-hidden="true">#</a> API</h2>
<p>ts-node's complete API is documented here: <a href="https://typestrong.org/ts-node/api/" target="_blank" rel="noopener noreferrer">API Docs<ExternalLinkIcon/></a></p>
<p>Here are a few highlights of what you can accomplish:</p>
<ul>
<li><a href="https://typestrong.org/ts-node/api/index.html#create" target="_blank" rel="noopener noreferrer"><code v-pre>create()</code><ExternalLinkIcon/></a> creates ts-node's compiler service without
registering any hooks.</li>
<li><a href="https://typestrong.org/ts-node/api/index.html#createRepl" target="_blank" rel="noopener noreferrer"><code v-pre>createRepl()</code><ExternalLinkIcon/></a> creates an instance of our REPL service, so
you can create your own TypeScript-powered REPLs.</li>
<li><a href="https://typestrong.org/ts-node/api/index.html#createEsmHooks" target="_blank" rel="noopener noreferrer"><code v-pre>createEsmHooks()</code><ExternalLinkIcon/></a> creates our ESM loader hooks,
suitable for composing with other loaders or augmenting with additional features.</li>
</ul>
<h1 id="recipes" tabindex="-1"><a class="header-anchor" href="#recipes" aria-hidden="true">#</a> Recipes</h1>
<h2 id="watching-and-restarting" tabindex="-1"><a class="header-anchor" href="#watching-and-restarting" aria-hidden="true">#</a> Watching and restarting</h2>
<p>ts-node focuses on adding first-class TypeScript support to node.  Watching files and code reloads are out of scope for the project.</p>
<p>If you want to restart the <code v-pre>ts-node</code> process on file change, existing node.js tools such as <a href="https://github.com/remy/nodemon" target="_blank" rel="noopener noreferrer">nodemon<ExternalLinkIcon/></a>, <a href="https://github.com/Qard/onchange" target="_blank" rel="noopener noreferrer">onchange<ExternalLinkIcon/></a> and <a href="https://github.com/fgnass/node-dev" target="_blank" rel="noopener noreferrer">node-dev<ExternalLinkIcon/></a> work.</p>
<p>There's also <a href="https://github.com/whitecolor/ts-node-dev" target="_blank" rel="noopener noreferrer"><code v-pre>ts-node-dev</code><ExternalLinkIcon/></a>, a modified version of <a href="https://github.com/fgnass/node-dev" target="_blank" rel="noopener noreferrer"><code v-pre>node-dev</code><ExternalLinkIcon/></a> using <code v-pre>ts-node</code> for compilation that will restart the process on file change. Note that <code v-pre>ts-node-dev</code> is incompatible with our native ESM loader.</p>
<h2 id="ava" tabindex="-1"><a class="header-anchor" href="#ava" aria-hidden="true">#</a> AVA</h2>
<p>Assuming you are configuring AVA via your <code v-pre>package.json</code>, add one of the following configurations.</p>
<h3 id="commonjs-1" tabindex="-1"><a class="header-anchor" href="#commonjs-1" aria-hidden="true">#</a> CommonJS</h3>
<p>Use this configuration if your <code v-pre>package.json</code> does not have <code v-pre>&quot;type&quot;: &quot;module&quot;</code>.</p>
<div class="language-jsonc ext-jsonc line-numbers-mode"><pre v-pre class="language-jsonc"><code>{
  &quot;ava&quot;: {
    &quot;extensions&quot;: [
      &quot;ts&quot;
    ],
    &quot;require&quot;: [
      &quot;ts-node/register&quot;
    ]
  }
}
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h3 id="native-ecmascript-modules-1" tabindex="-1"><a class="header-anchor" href="#native-ecmascript-modules-1" aria-hidden="true">#</a> Native ECMAScript modules</h3>
<p>This configuration is necessary if your <code v-pre>package.json</code> has <code v-pre>&quot;type&quot;: &quot;module&quot;</code>.</p>
<div class="language-jsonc ext-jsonc line-numbers-mode"><pre v-pre class="language-jsonc"><code>{
  &quot;ava&quot;: {
    &quot;extensions&quot;: {
      &quot;ts&quot;: &quot;module&quot;
    },
    &quot;nonSemVerExperiments&quot;: {
      &quot;configurableModuleFormat&quot;: true
    },
    &quot;nodeArguments&quot;: [
      &quot;--loader=ts-node/esm&quot;
    ]
  }
}
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="gulp" tabindex="-1"><a class="header-anchor" href="#gulp" aria-hidden="true">#</a> Gulp</h2>
<p>ts-node support is built-in to gulp.</p>
<div class="language-bash ext-sh line-numbers-mode"><pre v-pre class="language-bash"><code><span class="token comment"># Create a `gulpfile.ts` and run `gulp`.</span>
gulp
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>See also: https://gulpjs.com/docs/en/getting-started/javascript-and-gulpfiles#transpilation</p>
<h2 id="intellij-and-webstorm" tabindex="-1"><a class="header-anchor" href="#intellij-and-webstorm" aria-hidden="true">#</a> IntelliJ and Webstorm</h2>
<p>Create a new Node.js configuration and add <code v-pre>-r ts-node/register</code> to &quot;Node parameters.&quot;</p>
<p><strong>Note:</strong> If you are using the <code v-pre>--project &lt;tsconfig.json&gt;</code> command line argument as per the <a href="#configuration">Configuration Options</a>, and want to apply this same behavior when launching in IntelliJ, specify under &quot;Environment Variables&quot;: <code v-pre>TS_NODE_PROJECT=&lt;tsconfig.json&gt;</code>.</p>
<h2 id="mocha" tabindex="-1"><a class="header-anchor" href="#mocha" aria-hidden="true">#</a> Mocha</h2>
<h3 id="mocha-7-and-newer" tabindex="-1"><a class="header-anchor" href="#mocha-7-and-newer" aria-hidden="true">#</a> Mocha 7 and newer</h3>
<div class="language-bash ext-sh line-numbers-mode"><pre v-pre class="language-bash"><code>mocha --require ts-node/register --extensions ts,tsx --watch --watch-files src <span class="token string">'tests/**/*.{ts,tsx}'</span> <span class="token punctuation">[</span><span class="token punctuation">..</span>.args<span class="token punctuation">]</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Or specify options via your mocha config file.</p>
<div class="language-jsonc ext-jsonc line-numbers-mode"><pre v-pre class="language-jsonc"><code>{
  // Specify &quot;require&quot; for CommonJS
  &quot;require&quot;: &quot;ts-node/register&quot;,
  // Specify &quot;loader&quot; for native ESM
  &quot;loader&quot;: &quot;ts-node/esm&quot;,
  &quot;extensions&quot;: [&quot;ts&quot;, &quot;tsx&quot;],
  &quot;spec&quot;: [
    &quot;tests/**/*.spec.*&quot;
  ],
  &quot;watch-files&quot;: [
    &quot;src&quot;
  ]
}
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>See also: https://mochajs.org/#configuring-mocha-nodejs</p>
<h3 id="mocha-6" tabindex="-1"><a class="header-anchor" href="#mocha-6" aria-hidden="true">#</a> Mocha &lt;=6</h3>
<div class="language-bash ext-sh line-numbers-mode"><pre v-pre class="language-bash"><code>mocha --require ts-node/register --watch-extensions ts,tsx <span class="token string">"test/**/*.{ts,tsx}"</span> <span class="token punctuation">[</span><span class="token punctuation">..</span>.args<span class="token punctuation">]</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p><strong>Note:</strong> <code v-pre>--watch-extensions</code> is only used in <code v-pre>--watch</code> mode.</p>
<h2 id="tape" tabindex="-1"><a class="header-anchor" href="#tape" aria-hidden="true">#</a> Tape</h2>
<div class="language-bash ext-sh line-numbers-mode"><pre v-pre class="language-bash"><code>ts-node node_modules/tape/bin/tape <span class="token punctuation">[</span><span class="token punctuation">..</span>.args<span class="token punctuation">]</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><h2 id="visual-studio-code" tabindex="-1"><a class="header-anchor" href="#visual-studio-code" aria-hidden="true">#</a> Visual Studio Code</h2>
<p>Create a new Node.js debug configuration, add <code v-pre>-r ts-node/register</code> to node args and move the <code v-pre>program</code> to the <code v-pre>args</code> list (so VS Code doesn't look for <code v-pre>outFiles</code>).</p>
<div class="language-jsonc ext-jsonc line-numbers-mode"><pre v-pre class="language-jsonc"><code>{
    &quot;configurations&quot;: [{
        &quot;type&quot;: &quot;node&quot;,
        &quot;request&quot;: &quot;launch&quot;,
        &quot;name&quot;: &quot;Launch Program&quot;,
        &quot;runtimeArgs&quot;: [
            &quot;-r&quot;,
            &quot;ts-node/register&quot;
        ],
        &quot;args&quot;: [
            &quot;${workspaceFolder}/src/index.ts&quot;
        ]
    }],
}
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><strong>Note:</strong> If you are using the <code v-pre>--project &lt;tsconfig.json&gt;</code> command line argument as per the <a href="#configuration">Configuration Options</a>, and want to apply this same behavior when launching in VS Code, add an &quot;env&quot; key into the launch configuration: <code v-pre>&quot;env&quot;: { &quot;TS_NODE_PROJECT&quot;: &quot;&lt;tsconfig.json&gt;&quot; }</code>.</p>
<h2 id="other" tabindex="-1"><a class="header-anchor" href="#other" aria-hidden="true">#</a> Other</h2>
<p>In many cases, setting <a href="https://nodejs.org/api/cli.html#cli_node_options_options" target="_blank" rel="noopener noreferrer"><code v-pre>NODE_OPTIONS</code><ExternalLinkIcon/></a> will enable <code v-pre>ts-node</code> within other node tools, child processes, and worker threads.</p>
<div class="language-bash ext-sh line-numbers-mode"><pre v-pre class="language-bash"><code><span class="token assign-left variable">NODE_OPTIONS</span><span class="token operator">=</span><span class="token string">"-r ts-node/register"</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Or, if you require native ESM support:</p>
<div class="language-bash ext-sh line-numbers-mode"><pre v-pre class="language-bash"><code><span class="token assign-left variable">NODE_OPTIONS</span><span class="token operator">=</span><span class="token string">"--loader ts-node/esm"</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>This tells any node processes which receive this environment variable to install <code v-pre>ts-node</code>'s hooks before executing other code.</p>
<h1 id="license" tabindex="-1"><a class="header-anchor" href="#license" aria-hidden="true">#</a> License</h1>
<p>ts-node is licensed under the MIT license.  <a href="https://github.com/TypeStrong/ts-node/blob/main/LICENSE" target="_blank" rel="noopener noreferrer">MIT<ExternalLinkIcon/></a></p>
<p>ts-node includes source code from Node.js which is licensed under the MIT license.  <a href="https://raw.githubusercontent.com/nodejs/node/master/LICENSE" target="_blank" rel="noopener noreferrer">Node.js license information<ExternalLinkIcon/></a></p>
<p>ts-node includes source code from the TypeScript compiler which is licensed under the Apache License 2.0.  <a href="https://github.com/microsoft/TypeScript/blob/master/LICENSE.txt" target="_blank" rel="noopener noreferrer">TypeScript license information<ExternalLinkIcon/></a></p>
</div></template>
